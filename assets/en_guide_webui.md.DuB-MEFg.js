import{_ as a,o,c as t,a2 as n}from"./chunks/framework.CQdiwDJy.js";const g=JSON.parse('{"title":"Web UI","description":"","frontmatter":{},"headers":[],"relativePath":"en/guide/webui.md","filePath":"en/guide/webui.md","lastUpdated":1788601681000}'),i={name:"en/guide/webui.md"};function s(r,e,d,l,c,h){return o(),t("div",null,[...e[0]||(e[0]=[n('<h1 id="web-ui" tabindex="-1">Web UI <a class="header-anchor" href="#web-ui" aria-label="Permalink to &quot;Web UI&quot;">​</a></h1><p><code>tau web</code> starts the local web interface (default <code>127.0.0.1:8787</code>, never exposed to the network): plan cards, thinking panels and live tool calls in the browser, backed by the exact same engine and safety gate as the CLI.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tau</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> web</span></span></code></pre></div><h2 id="two-modes" tabindex="-1">Two modes <a class="header-anchor" href="#two-modes" aria-label="Permalink to &quot;Two modes&quot;">​</a></h2><ul><li><strong>plan mode</strong>: intent → plan card → review badge → you press Run → live result card. High-risk plans get a card-local explicit checkbox; deny verdicts hard-disable Run.</li><li><strong>agent mode</strong>: a multi-round goal timeline. Each round shows its plan, review badge and live step output; medium+ rounds pause inline with Approve / Refuse on the card — never a blanket pre-approval.</li></ul><h2 id="thinking-panels-v0-5-0" tabindex="-1">Thinking panels (v0.5.0) <a class="header-anchor" href="#thinking-panels-v0-5-0" aria-label="Permalink to &quot;Thinking panels (v0.5.0)&quot;">​</a></h2><p>While planning, the provider&#39;s reasoning streams into a collapsible panel (pinned open while streaming, collapsed to &quot;Thought for Ns&quot; when done); in agent mode each round&#39;s planning and reflection thinking collapse into their own rounds. Token usage shows in the card eyebrow.</p><h2 id="tool-call-cards-and-the-file-viewer-v0-5-0" tabindex="-1">Tool call cards and the file viewer (v0.5.0) <a class="header-anchor" href="#tool-call-cards-and-the-file-viewer-v0-5-0" aria-label="Permalink to &quot;Tool call cards and the file viewer (v0.5.0)&quot;">​</a></h2><p>Tool steps in agent rounds render as structured cards: tool name, risk badge, collapsible args JSON, live output. <code>file.read</code> steps render as a file viewer instead — path, language chip and a shiki-highlighted body, with language detection shared with the tool layer.</p><h2 id="slash-command-menu-v0-6-0" tabindex="-1">Slash command menu (v0.6.0) <a class="header-anchor" href="#slash-command-menu-v0-6-0" aria-label="Permalink to &quot;Slash command menu (v0.6.0)&quot;">​</a></h2><p>Typing <code>/</code> (or a prefix like <code>/th</code>) in the composer opens a floating command menu — <code>↑</code>/<code>↓</code> move, <code>Tab</code>/<code>Enter</code> run, <code>Esc</code> dismisses; mouse hover/click work too. Commands execute CLIENT-side (<code>/new</code> thread, <code>/theme</code>, <code>/plan</code>, <code>/agent</code>, <code>/help</code>, <code>/settings</code>, …) and are <strong>never sent to the AI as intents</strong>; the menu reads the same shared command catalog as the TUI palette (served via <code>GET /api/commands</code>), so what is shown can never drift from what runs.</p><h2 id="image-attachments-v0-6-0" tabindex="-1">Image attachments (v0.6.0) <a class="header-anchor" href="#image-attachments-v0-6-0" aria-label="Permalink to &quot;Image attachments (v0.6.0)&quot;">​</a></h2><p>The paperclip button, clipboard paste and drag-and-drop all feed one validated draft list: PNG/JPEG/WebP/GIF, up to 4 images, max 4 MB each; drafts render as removable preview chips, and sending images without text uses an explicit default intent. Payloads ride the request only — user cards keep name/type/size meta (thumbnails are session-only), and nothing image-shaped ever enters the NDJSON event stream or localStorage. Vision-capable providers (openai/anthropic/gemini/ollama) receive the images in their native wire shape; text-only providers get an honest &quot;image was dropped&quot; annotation instead of pretending to see. The server re-validates independently: media-type whitelist, count/size caps, and a magic-number probe (a renamed text file cannot masquerade as an image).</p><h2 id="sandboxed-previews-and-native-viewing-v0-6-0" tabindex="-1">Sandboxed previews and native viewing (v0.6.0) <a class="header-anchor" href="#sandboxed-previews-and-native-viewing-v0-6-0" aria-label="Permalink to &quot;Sandboxed previews and native viewing (v0.6.0)&quot;">​</a></h2><ul><li><strong>HTML preview</strong>: ```html fenced blocks in results/goal answers gain a preview toggle — the code opens in an <code>&lt;iframe sandbox=&quot;allow-scripts&quot;&gt;</code> (no allow-same-origin; an opaque origin cannot touch the parent page, cookies or storage). The escape-first markdown pipeline is untouched — the preview is a separate sandbox channel.</li><li><strong>PDF / image native viewing</strong>: <code>file.read</code> of a PDF or an image no longer shows binary-as-text — the file streams through the read-only <code>GET /api/file</code> route into the browser&#39;s native viewer (<code>&lt;embed&gt;</code> / <code>&lt;img&gt;</code>). The route is workspace-contained (reusing the write tools&#39; own containment helpers plus a realpath re-check that closes symlink escape), size-capped at 8 MB, serves a conservative mime whitelist (never html/js/svg or anything executable), and answers 403/404/413 with plain JSON.</li></ul><h2 id="local-sessions" tabindex="-1">Local sessions <a class="header-anchor" href="#local-sessions" aria-label="Permalink to &quot;Local sessions&quot;">​</a></h2><p>Conversation threads persist in browser localStorage (key <code>tau-webui-threads-v1</code>, cap 50); the server history is the durable record; the card schema is additive-only. The settings panel shows the <strong>redacted</strong> effective config (API keys are always masked).</p><h2 id="provider-setup-v0-6-1" tabindex="-1">Provider setup (v0.6.1) <a class="header-anchor" href="#provider-setup-v0-6-1" aria-label="Permalink to &quot;Provider setup (v0.6.1)&quot;">​</a></h2><h2 id="model-thinking-selection-v0-6-2" tabindex="-1">Model &amp; thinking selection (v0.6.2) <a class="header-anchor" href="#model-thinking-selection-v0-6-2" aria-label="Permalink to &quot;Model &amp; thinking selection (v0.6.2)&quot;">​</a></h2><p>The settings panel&#39;s <strong>provider</strong> section is now selectable: the model row is a catalog-backed dropdown (<code>GET /api/models</code>, refresh button included) writing through <code>POST /api/config/model</code>, and a <strong>thinking</strong> row renders mode (on/off) and effort (low/medium/high) mini-pickers straight from the server&#39;s capability table — providers without a knob render the honest &quot;no thinking knobs&quot; note instead of dead controls. Both writes go through the same <code>setConfigValue</code> channel the CLI uses; the gate and risk policy stay immutable from the browser.</p><p>The settings panel still carries a <strong>provider setup</strong> block — the credential slice:</p><ul><li><strong>Model link lookup</strong>: picking a provider prefills the endpoint (from a server-sent catalog that is parity-checked against the live provider registry) — no URL typing; an advanced disclosure allows a custom endpoint (OpenAI-shaped providers write <code>providers.&lt;name&gt;.baseUrl</code>, Ollama writes <code>host</code>).</li><li><strong>Paste-only key</strong>: follow the per-provider console link, paste the key, save — through <code>POST /api/config/provider</code>, the same <code>setConfigValue</code> channel <code>tau provider set-key</code> uses (chmod-0600 config file). Saving also refreshes the model catalog and can activate the provider in one step (keyless mock/ollama/zai need no key).</li><li><strong>Privacy masking</strong>: the key input is a <code>password</code> field; the &quot;show&quot; toggle re-masks itself after 8 seconds; a saved key renders only as the server&#39;s mask (<code>sk-***last4</code>) — plaintext exists only in the localhost request body and the 0600 config file, never echoed, never logged. The gate and risk policy remain <strong>immutable</strong> from the browser.</li></ul>',22)])])}const u=a(i,[["render",s]]);export{g as __pageData,u as default};
