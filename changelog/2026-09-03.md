# Changelog — 2026-09-03

## 变更摘要

维护者一次性下达的复合需求（WebUI 思考/流式/工具调用/高亮 + VitePress
文档站 + 文档 skill + 工具增量 + AI provider 重构）已按
AGENTS/collaboration.md §3 分解为 6 个单元并公示（Issues #106–#112，
分解方案公示见 #106）。本日文件按实施顺序记录各单元批次。

## 批次 1 — Unit 1：AI provider 统一流式抽象 + anthropic/gemini（refactor）

### 变更摘要

参考 OpenAI Chat Completions SSE、Anthropic Messages SSE、Google Gemini
`streamGenerateContent alt=sse`、Ollama `/api/chat` NDJSON 四家 wire 协议，
将 `@tau/ai` 重构到统一流式抽象上：

- 新增 `packages/ai/src/chat-stream.ts`：四个 wire 各一个共享消费者，
  全部折叠为 provider 无关的 `ProviderStreamEvent` 协议
  （`reasoning_delta` / `text_delta` / `usage`，类型落 `@tau/core`）。
- `AIProvider` 新增**可选** `planStream(ctx, onEvent?)`：流式产出
  reasoning/text 增量并返回与 `plan()` 完全相同（同一 `validatePlanResponse`
  门禁）的 Plan；未实现的 provider 零行为变化。
- 七个 provider 全部落位：openai（SSE + `reasoning_content`）、deepseek
  （harness `StreamChunk` 转发 + 直连路径增量转发，两条路径语义一致）、
  ollama（NDJSON 流式 + `providers.ollama.think` 思考开关）、zai（SDK 非
  流式 → 单次诚实 text 事件，不发明 reasoning）、mock（确定性罐头事件序
  列，离线 demo/测试/截图全链路可用）、**新增 anthropic**（Messages API：
  `x-api-key` + `anthropic-version`，流式 wire，`/v1/models` 发现，
  `providers.anthropic.thinking` + `thinkingBudget` 开启扩展思考且按 API
  契约在思考模式下省略 temperature）、**新增 gemini**（Generative Language
  REST：`x-goog-api-key`，`systemInstruction`，JSON 模式
  `responseMimeType`，thought parts → reasoning，可选 thinkingBudget，
  `GOOGLE_API_KEY`/`GEMINI_API_KEY` 双环境变量回退）。
- `normalizeUsage` 扩展支持 DeepSeek harness / Gemini / Ollama usage 形状；
  `PROVIDER_FIELDS` 增加 `think` / `thinking` / `thinkingBudget`。
- 反射能力扩展：anthropic、gemini 同 wire 实现 `reflect()`。
- registry 注册 7 个内置 provider；双语 README provider 表、
  `packages/ai/README.md` 公开 API、`AGENTS/ai-integration.md` 规则本同步。

### 变更类型：refactor（接口 additive 扩展 + 新增 provider）

| 文件                                       | 变更                                                                                                                       |
| ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- |
| `packages/core/src/types.ts`               | 新增 `ProviderStreamEvent` / `ProviderStreamHandler`；`AIProvider.planStream?`；registry key 注释更新                      |
| `packages/core/src/config/store.ts`        | `PROVIDER_FIELDS` 增加 `think`/`thinking`/`thinkingBudget`                                                                 |
| `packages/ai/src/chat-stream.ts`           | 新增：统一流式 wire 层（4 个消费者 + SSE/NDJSON 迭代器）                                                                   |
| `packages/ai/src/usage.ts`                 | `normalizeUsage` 支持 4 家 wire 形状                                                                                       |
| `packages/ai/src/providers/openai.ts`      | `planStream`（SSE + reasoning_content）                                                                                    |
| `packages/ai/src/providers/deepseek.ts`    | `collectStreamText` 增量转发；harness 路径 chunk 转发；`planStream` 双路径                                                 |
| `packages/ai/src/providers/ollama.ts`      | `planStream`（NDJSON + think 开关）                                                                                        |
| `packages/ai/src/providers/zai.ts`         | `planStream` 单次诚实降级                                                                                                  |
| `packages/ai/src/providers/mock.ts`        | 确定性 `planStream` + `chunkText` 助手                                                                                     |
| `packages/ai/src/providers/anthropic.ts`   | 新增：Anthropic Messages provider（plan/planStream/reflect/listModels）                                                    |
| `packages/ai/src/providers/gemini.ts`      | 新增：Google Gemini provider（plan/planStream/reflect/listModels）                                                         |
| `packages/ai/src/registry.ts` / `index.ts` | 注册 + barrel                                                                                                              |
| 测试                                       | `chat-stream.test.ts`（14）、`anthropic-provider.test.ts`（8）、`gemini-provider.test.ts`（7）、`plan-stream.test.ts`（6） |
| 快照/既有测试                              | `provider-cli.test.ts`（7 个 provider）、webui e2e 快照（providers 列表）                                                  |
| 文档                                       | `README.md` ×2、`packages/ai/README.md`、`AGENTS/ai-integration.md`                                                        |

### 相关 Issue/PR

- Issue #107（Unit 1）；分解公示 #106。分支：`feat/ai-provider-refactor` →
  集成分支 `release/v0.5.0-webui-ai-docs`。

### 影响范围简述

- 行为：AIProvider 接口 additive（可选 `planStream`）；7 个 provider 注册；
  新配置字段；planning 默认路径（`plan()`）行为零变化；plan 契约与安全
  审查零变化。
- 文档：双语 README、包 README、AGENTS 规则本已同步。

### 测试与门禁

- lint=pass（0 warnings 0 errors）typecheck=pass test=pass（509 tests，
  其中新增 35）format=pass build=pass（tsdown + tui + webui）

### 设计决策

- Anthropic 的 usage 字段名（`input_tokens`/`output_tokens`）与 OpenAI
  （`prompt_tokens`/`completion_tokens`）不同，Anthropic 解析器内直接读
  原始字段而非经 `normalizeUsage`（后者保持 OpenAI/harness/Gemini/Ollama
  四形状）。
- 流式路径统一"always-stream"：无观察者时 delta 直接丢弃，即缓冲语义，
  单一 wire 路径服务 plan 与 planStream 两个能力。
- 运行时依赖零新增（坚持纯 fetch），未启用 optionalDependency 豁免。

## 批次 2 — Unit 2：agent 流式规划管线（feat）

### 变更摘要

| 文件                                                          | 变更                                                                                                                                                                                |
| ------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/core/src/types.ts`                                  | `AIProvider` 新增可选能力 `reflectStream?`                                                                                                                                          |
| `packages/agent/src/pipeline.ts`                              | 提取共享前置 `preparePlanning()`（catalog + provider 解析 + 可用性检查）；新增 `planIntentStream(intent, options, onEvent?)`                                                        |
| `packages/agent/src/session.ts`                               | 新增 `planAndReviewStream()`：流式规划接同一确定性 review                                                                                                                           |
| `packages/agent/src/loop.ts`                                  | `RunGoalOptions.onPlanStream?`：runGoal 逐轮中继 provider 流事件；reflect 事件带轮次标记                                                                                            |
| `packages/ai/src/providers/{openai,anthropic,gemini,mock}.ts` | 补齐 `reflectStream`（deepseek/ollama/zai 无此能力，自动回退）                                                                                                                      |
| 测试                                                          | `packages/agent/tests/plan-stream.test.ts` 新增 5 个：事件序列一致性、无 planStream 零事件回退、planAndReviewStream review、runGoal 全轮脚本（思考+文本+reflect）、无观察者缓冲路径 |

### 相关 Issue/PR

- Issue #108（Unit 2）；分解公示 #106。分支：`feat/agent-plan-stream` →
  集成分支 `release/v0.5.0-webui-ai-docs`。依赖 Unit 1（#107）的
  `planStream`/`ProviderStreamEvent`。

### 影响范围简述

- 行为：`runGoal` 在传入 `onPlanStream` 时逐轮中继 reasoning/text/usage
  流事件；未传入观察者时执行路径与原版完全一致（零行为变化）。旧
  provider 缺失可选能力时自动回退缓冲 `plan()`/`reflect()`。
- 安全：plan 契约与安全审查零削弱——流式仅改变文本到达方式，组装后的
  计划文本仍走 `validatePlanResponse()`（zod strict JSON）与
  `reviewPlan()`（确定性审查）；AI 永不自我评分的原则不变。

### 测试与门禁

- lint=pass（0 warnings 0 errors）typecheck=pass test=pass（514 tests，
  其中新增 5）format=pass build=pass（tsdown + tui + webui）

### 设计决策

- 复用 Unit 1 的 `ProviderStreamEvent` 形状，agent 层不发明新事件类型，
  Unit 4 的 WebUI 可直接透传到浏览器。
- 回退策略（能力缺失 → 缓冲调用）保证 deepseek/ollama/zai 等未实现
  `reflectStream` 的 provider 完全不受影响；`planIntentStream` 与
  `planIntent` 共享 `preparePlanning()`，避免两套前置逻辑漂移。
- reflect 流事件复用同一 `onPlanStream` 通道并以 `round + 1` 标记，
  调用方（Unit 4 WebUI）可区分"规划思考"与"反思思考"两个阶段。

## 批次 3 — Unit 3：file.read 语言元数据（feat）

### 变更摘要

| 文件                              | 变更                                                                                                                                                                                                       |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/tools/src/file.ts`      | 新增 `languageForFile(name)`：文件名 → shiki 兼容语言 id（扩展名映射 + Dockerfile/Makefile 等整名匹配，未知回落 `text`）；`file.read` 结构化结果新增 `path`/`language`；file.read 描述补一句语言元数据说明 |
| `packages/tools/src/bootstrap.ts` | barrel 导出 `languageForFile`（WebUI 文件查看器复用同一检测）                                                                                                                                              |
| 测试                              | `tools-file.test.ts` 新增 6 个：readTool data 的 path/language、未知扩展回落、常见扩展映射、整名匹配（大小写不敏感）、多点文件名取最后扩展、无扩展/dotfile/未知回落                                        |

### 相关 Issue/PR

- Issue #109（Unit 3）；分解公示 #106。分支：`feat/tools-file-language`
  → 集成分支。服务 Unit 4（#110）的文件查看器高亮。

### 影响范围简述

- 行为：`file.read` 的文本输出零变化（AI 可见格式稳定）；结构化 `data`
  仅新增字段，additive。风险等级、参数 schema 均不变。
- 词汇表刻意保守：宁可不高亮，也不错标语言（未知 → `text`）。

### 测试与门禁

- lint=pass（0 warnings 0 errors）typecheck=pass test=pass（520 tests，
  其中新增 6）format=pass build=pass

### 设计决策

- 语言 id 直接对齐 shiki（WebUI 高亮器的选型），工具层与前端共享同一
  `languageForFile()`，避免两套扩展名表漂移。
- 整名匹配（`Dockerfile`/`Makefile`/`Gemfile`）优先于扩展名，大小写
  不敏感；dotfile（`.gitignore`）视为无扩展名。
- 放在 `@tau/tools` 而非 webui 本地：检测逻辑是工具语义的一部分，
  由 file.read 的生产者定义，消费者（WebUI）只读取。

---

AI-Generated: Unit 3 — file.read 语言元数据（languageForFile 映射 + readTool data.path/language + barrel 导出 + 测试）
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: packages/tools（file.ts + bootstrap.ts + tests）、CHANGELOG.md、changelog/2026-09-03.md
AI-gate: lint=pass typecheck=pass test=pass (520 tests) format=pass build=pass
