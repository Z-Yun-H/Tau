# Changelog — 2026-09-05

## 变更摘要

来源指令：维护者一次性下达的复合需求（① 删除死代码；② WebUI 页面高度固定、
页面总高度不由内部组件决定；③ 设置界面提供 AI 模型链接查询 + 仅粘贴 API key

- 防窥掩码；④ 组件抽离重构与组件测试），并要求按仓库 PR 提交规范执行。

按 AGENTS/collaboration.md §3 完成复合需求自动分解并公示为 Issue #149–#152：
U1 死代码清理（refactor）→ U2 页面高度固定（fix）→ U3 组件抽离（refactor）→
U4 设置界面 provider 配置（feat）。涉及子系统：app/webui（client + server 同
包），不构成跨 3+ 子系统的大型重构，单元 PR base = main。

最终状态：批次 1–4 全部实施完毕，4 个 PR 待人工评审合并（AI 不自行合并）。

## 批次 1 — webui 死代码清理（refactor）

### 变更摘要

对 `app/webui` 做全量死代码扫描（导出符号全仓引用分析 + CSS 类使用核查 +
依赖使用核查），甄别后删除确认的死代码，行为零变化：

- `client/theme.css`：删除 4 个无引用全局类 —— `.tau-chrome-text` 与
  `.tau-chrome-text-hover`（品牌标记的 chrome 文字已由 `StatusHeader.vue`
  局部类 `.brand-chrome` 的垂直 sheen 渐变实现，全局类遗留未删）；
  `.tau-surface-raised` 与 `.tau-surface-floating`（表面家族三档中实际只有
  `.tau-surface` 被使用，组件对更高层级直接使用 `--tau-elev-2/3` 变量）。
- `client/lib/api.ts`：删除类型 `CommandsPayload`（全仓含测试零引用；
  `GET /api/commands` 返回形状实际由 `CommandInfo[]` 直接承载）。
- `app/webui/DESIGN.md`：表面家族与品牌标记两处描述同步实现。

### 甄别记录（确认不是死代码、不删）

- `packages/*` 全部 barrel 导出扫描后未发现「全仓零引用且文件内也未使用」的
  真死代码；若干仅文件内使用的类型属于 `@tau/*` 已发布家族的公共 API 面，
  删除将造成 API 破坏，超出本单元范围。
- `packages/engine/src/safety.ts` 防御性模式表按黄金规则 1 不动。
- `app/webui/src/server.ts` 的 `FILE_PREVIEW_MIME` 导出是 client/server
  parity 测试锚点（活代码）。
- `app/webui` 运行时/开发依赖均在使用（shiki、@vueuse/core、@tau/* 等），
  无未使用依赖。

### 变更类型：refactor

| 文件                        | 变更                             |
| --------------------------- | -------------------------------- |
| app/webui/client/theme.css  | 删除 4 个未使用全局类，注释同步  |
| app/webui/client/lib/api.ts | 删除未使用类型 `CommandsPayload` |
| app/webui/DESIGN.md         | 表面家族/品牌标记描述与实现对齐  |
| CHANGELOG.md                | Unreleased 片段（Changed）       |

### 相关 Issue/PR

Issue #149；分支 `refactor/webui-dead-code`；PR 见正文索引。无跨单元依赖，
U2 因同文件（theme.css）随后续批次落地。

### 影响范围简述

运行时行为零变化（删除的类/类型从未被引用，快照与 DOM 输出不变）；公共 API
零变化（webui 客户端非发布面）；文档同步 DESIGN.md + CHANGELOG。

### 测试与门禁

lint=pass typecheck=pass test=pass (639 passed, 63 files) build=pass
（webui client 构建零 unmatched-utility 警告）

### 设计决策

- `.tau-surface-raised` / `.tau-surface-floating` 虽在 DESIGN.md 中有规划性
  描述，但按「no dead code」规则删除并同步文档；如未来需要更高表面档位，直接
  组合 `.tau-surface` + elevation token 即可，无需预置类。
- 扫描范围聚焦本次触达子系统 `app/webui`；`packages/*` 的甄别结论记录于
  Issue #149，供维护者后续决策。

---

AI-Generated: 批次 1 — webui 死代码清理（4 个未使用全局 CSS 类 + 1 个未使用类型 + DESIGN.md 同步，行为零变化）
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: app/webui/client/theme.css, app/webui/client/lib/api.ts, app/webui/DESIGN.md, CHANGELOG.md, changelog/2026-09-05.md
AI-gate: lint=pass typecheck=pass test=pass (639) build=pass

---

## 批次 2 — webui 页面高度固定 viewport-lock（fix）

### 变更摘要

修复「页面总高度随内部组件高度增长」的布局缺陷：`body`/`#app` 原为
`min-height: 100dvh`，长会话流会撑出整页滚动条，与内部列滚动并存（双滚动）。
SKILL.md/DESIGN.md 布局契约早已声明「viewport-locked (h-dvh app shell)」，
本批次让实现与规范一致：

- `body`：`height: 100dvh` + `overflow: hidden` —— 页面总高度恒等于视口。
- `#app`：`height: 100%`，删除 `min-height`。
- 会话流列 `<section>` 增加 `flex-1`（窄屏 flex 语义；lg 网格下惰性），
  使窄屏下滚动收敛到 `.stream-scroll` 内部；composer 成为列的最后一个
  flex 子元素，常驻可见（删除原 sticky 定位——在非滚动容器中 sticky 已是
  死规则）。
- 文档同步：SKILL.md 布局契约、DESIGN.md §5 布局与 §6.3 composer 的窄屏
  行为由「one scrolling flow / sticky」更新为「viewport-locked at all
  breakpoints; the conversation stream scrolls」。

### 变更类型：fix（含显示逻辑重构）

| 文件                                    | 变更                                                      |
| --------------------------------------- | --------------------------------------------------------- |
| app/webui/client/theme.css              | body/#app viewport-lock                                   |
| app/webui/client/App.vue                | 流列 flex-1、composer dock 注释与 sticky 移除、头注释更新 |
| app/webui/tests/client-viewport.test.ts | 新增 Chromium DOM 契约测试                                |
| app/webui/SKILL.md, app/webui/DESIGN.md | 布局契约同步                                              |
| CHANGELOG.md                            | Unreleased 片段（Fixed）                                  |

### 相关 Issue/PR

Issue #150；分支 `fix/webui-viewport-lock`（堆叠于 `refactor/webui-dead-code`，
因 theme.css/App.vue 同文件）。合并次序：#153（批次 1）之后。

### 影响范围简述

窄屏布局行为变化（页面整体滚动 → 列内滚动、composer 由 sticky 变常驻）——
维护者明确要求的重构方向；lg+ 三列行为不变（原本的列滚动此时才真正被锁住）。
文档层（SKILL/DESIGN）同步；用户可见面 README 无布局细节，无需改动。

### 测试与门禁

lint=pass typecheck=pass test=pass（含新增 client-viewport 契约测试：
三断点断言页面不可滚动、流列承接溢出、窄屏 composer 可见）
build=pass

### 设计决策

- 用 DOM 契约测试而非快照测试锁定不变量（页面高度 == 视口），测试里以纯
  DOM 追加行模拟长会话，避免与应用行为耦合。
- 删除窄屏 sticky composer：viewport-lock 后 sticky 处于非滚动容器中属于
  死规则，按 no-dead-code 规则一并清理，行为由 flex 布局自然达成。

---

AI-Generated: 批次 2 — webui 页面高度固定（body/#app viewport-lock + 窄屏滚动收敛到流列 + composer 常驻 + 布局契约文档同步 + Chromium 契约测试）
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: app/webui/client/theme.css, app/webui/client/App.vue, app/webui/tests/client-viewport.test.ts, app/webui/SKILL.md, app/webui/DESIGN.md, CHANGELOG.md, changelog/2026-09-05.md
AI-gate: lint=pass typecheck=pass test=pass (640) build=pass

---

## 批次 3 — webui 组件抽离：会话流 / 弹层切换 + 组件测试（refactor）

### 变更摘要

App.vue 原内联「内容 chat 栏」（用户气泡 + 四类卡片分发 + EmptyState +
两个自动滚动 watcher）与「弹层切换」（shortcuts/settings 开关 + Esc 关闭），
本次抽出为独立组件，App.vue 收敛为纯组合（script 128 行）：

- `components/ConversationStream.vue`：会话流列（无 props，状态来自
  usePlanFlow 单例）；自动滚动所依赖的流尺寸累积抽为纯函数
  `streamVolume()`（plan-flow.ts 内，node 可测）。
- `components/ModalLayer.vue` + `composables/ui-state.ts`：弹层切换状态机
  （模块单例，与 session/plan-flow 同模式）；Esc 走 `closeOverlays()`；
  快捷键主循环、斜杠命令、头部按钮翻转同一组 refs。
- 组件测试（依赖冻结，不引入 @vue/test-utils）：
  - `stream-volume.test.ts`（node）：streamVolume 纯函数 4 例 +
    useUiState 状态机 4 例（含模块单例语义）。
  - `client-components-dom.test.ts`（Chromium，无浏览器自动跳过）：
    ModalLayer 键盘契约（Ctrl+, → settings、? → shortcuts、一次 Esc 全关）、
    ConversationStream 全链路渲染（气泡 → Run plan → result）、自动滚动
    follow（流列滚自身、页面不动）。

### 变更类型：refactor

| 文件                                               | 变更                       |
| -------------------------------------------------- | -------------------------- |
| app/webui/client/components/ConversationStream.vue | 新增（自 App.vue 抽出）    |
| app/webui/client/components/ModalLayer.vue         | 新增                       |
| app/webui/client/composables/ui-state.ts           | 新增                       |
| app/webui/client/composables/plan-flow.ts          | 新增纯函数 streamVolume    |
| app/webui/client/App.vue                           | 收敛为纯组合（样式随迁）   |
| app/webui/tests/stream-volume.test.ts              | 新增（node，8 例）         |
| app/webui/tests/client-components-dom.test.ts      | 新增（Chromium，3 例）     |
| app/webui/SKILL.md                                 | 组件清单 + 测试行同步      |
| CHANGELOG.md                                       | Unreleased 片段（Changed） |

### 相关 Issue/PR

Issue #151；分支 `refactor/webui-extract-components`（堆叠于
`fix/webui-viewport-lock`）。合并次序：#154 之后。

### 影响范围简述

渲染输出零变化（既有 client 字节级快照不破裂为证）；键盘契约行为不变；
localStorage 键不变。SKILL.md 组件清单同步。新增测试 11 例。

### 测试与门禁

lint=pass typecheck=pass test=pass（640 → 651：+11 新例；快照全保留）
build=pass

### 设计决策

- streamVolume 放 plan-flow.ts（与 CardState 同处，类似 priorTurnsOf 的
  纯助手），不另设 lib 文件。
- 不引入 @vue/test-utils（运行时依赖冻结 + 技术选型冻结）：组件行为测试
  走既有 Chromium DOM 测试模式，纯逻辑走 node 单测——两层覆盖。

---

AI-Generated: 批次 3 — webui 组件抽离（ConversationStream + ModalLayer/ui-state，App.vue 纯组合化，新增 11 例组件测试，渲染零变化）
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: app/webui/client/components/{ConversationStream,ModalLayer}.vue, app/webui/client/composables/{ui-state.ts,plan-flow.ts}, app/webui/client/App.vue, app/webui/tests/{stream-volume,client-components-dom}.test.ts, app/webui/SKILL.md, CHANGELOG.md, changelog/2026-09-05.md
AI-gate: lint=pass typecheck=pass test=pass (651) build=pass
