# Changelog — 2026-09-05

## 变更摘要

来源指令：维护者一次性下达的复合需求（① 删除死代码；② WebUI 页面高度固定、
页面总高度不由内部组件决定；③ 设置界面提供 AI 模型链接查询 + 仅粘贴 API key

- 防窥掩码；④ 组件抽离重构与组件测试），并要求按仓库 PR 提交规范执行。

按 AGENTS/collaboration.md §3 完成复合需求自动分解并公示为 Issue #149–#152：
U1 死代码清理（refactor）→ U2 页面高度固定（fix）→ U3 组件抽离（refactor）→
U4 设置界面 provider 配置（feat）。涉及子系统：app/webui（client + server 同
包），不构成跨 3+ 子系统的大型重构，单元 PR base = main。

最终状态：批次 1–4 全部实施完毕，4 个 PR 待人工评审合并（AI 不自行合并）。

## 批次 1 — webui 死代码清理（refactor）

### 变更摘要

对 `app/webui` 做全量死代码扫描（导出符号全仓引用分析 + CSS 类使用核查 +
依赖使用核查），甄别后删除确认的死代码，行为零变化：

- `client/theme.css`：删除 4 个无引用全局类 —— `.tau-chrome-text` 与
  `.tau-chrome-text-hover`（品牌标记的 chrome 文字已由 `StatusHeader.vue`
  局部类 `.brand-chrome` 的垂直 sheen 渐变实现，全局类遗留未删）；
  `.tau-surface-raised` 与 `.tau-surface-floating`（表面家族三档中实际只有
  `.tau-surface` 被使用，组件对更高层级直接使用 `--tau-elev-2/3` 变量）。
- `client/lib/api.ts`：删除类型 `CommandsPayload`（全仓含测试零引用；
  `GET /api/commands` 返回形状实际由 `CommandInfo[]` 直接承载）。
- `app/webui/DESIGN.md`：表面家族与品牌标记两处描述同步实现。

### 甄别记录（确认不是死代码、不删）

- `packages/*` 全部 barrel 导出扫描后未发现「全仓零引用且文件内也未使用」的
  真死代码；若干仅文件内使用的类型属于 `@tau/*` 已发布家族的公共 API 面，
  删除将造成 API 破坏，超出本单元范围。
- `packages/engine/src/safety.ts` 防御性模式表按黄金规则 1 不动。
- `app/webui/src/server.ts` 的 `FILE_PREVIEW_MIME` 导出是 client/server
  parity 测试锚点（活代码）。
- `app/webui` 运行时/开发依赖均在使用（shiki、@vueuse/core、@tau/* 等），
  无未使用依赖。

### 变更类型：refactor

| 文件                        | 变更                             |
| --------------------------- | -------------------------------- |
| app/webui/client/theme.css  | 删除 4 个未使用全局类，注释同步  |
| app/webui/client/lib/api.ts | 删除未使用类型 `CommandsPayload` |
| app/webui/DESIGN.md         | 表面家族/品牌标记描述与实现对齐  |
| CHANGELOG.md                | Unreleased 片段（Changed）       |

### 相关 Issue/PR

Issue #149；分支 `refactor/webui-dead-code`；PR 见正文索引。无跨单元依赖，
U2 因同文件（theme.css）随后续批次落地。

### 影响范围简述

运行时行为零变化（删除的类/类型从未被引用，快照与 DOM 输出不变）；公共 API
零变化（webui 客户端非发布面）；文档同步 DESIGN.md + CHANGELOG。

### 测试与门禁

lint=pass typecheck=pass test=pass (639 passed, 63 files) build=pass
（webui client 构建零 unmatched-utility 警告）

### 设计决策

- `.tau-surface-raised` / `.tau-surface-floating` 虽在 DESIGN.md 中有规划性
  描述，但按「no dead code」规则删除并同步文档；如未来需要更高表面档位，直接
  组合 `.tau-surface` + elevation token 即可，无需预置类。
- 扫描范围聚焦本次触达子系统 `app/webui`；`packages/*` 的甄别结论记录于
  Issue #149，供维护者后续决策。

---

AI-Generated: 批次 1 — webui 死代码清理（4 个未使用全局 CSS 类 + 1 个未使用类型 + DESIGN.md 同步，行为零变化）
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: app/webui/client/theme.css, app/webui/client/lib/api.ts, app/webui/DESIGN.md, CHANGELOG.md, changelog/2026-09-05.md
AI-gate: lint=pass typecheck=pass test=pass (639) build=pass

---

## 批次 2 — webui 页面高度固定 viewport-lock（fix）

### 变更摘要

修复「页面总高度随内部组件高度增长」的布局缺陷：`body`/`#app` 原为
`min-height: 100dvh`，长会话流会撑出整页滚动条，与内部列滚动并存（双滚动）。
SKILL.md/DESIGN.md 布局契约早已声明「viewport-locked (h-dvh app shell)」，
本批次让实现与规范一致：

- `body`：`height: 100dvh` + `overflow: hidden` —— 页面总高度恒等于视口。
- `#app`：`height: 100%`，删除 `min-height`。
- 会话流列 `<section>` 增加 `flex-1`（窄屏 flex 语义；lg 网格下惰性），
  使窄屏下滚动收敛到 `.stream-scroll` 内部；composer 成为列的最后一个
  flex 子元素，常驻可见（删除原 sticky 定位——在非滚动容器中 sticky 已是
  死规则）。
- 文档同步：SKILL.md 布局契约、DESIGN.md §5 布局与 §6.3 composer 的窄屏
  行为由「one scrolling flow / sticky」更新为「viewport-locked at all
  breakpoints; the conversation stream scrolls」。

### 变更类型：fix（含显示逻辑重构）

| 文件                                    | 变更                                                      |
| --------------------------------------- | --------------------------------------------------------- |
| app/webui/client/theme.css              | body/#app viewport-lock                                   |
| app/webui/client/App.vue                | 流列 flex-1、composer dock 注释与 sticky 移除、头注释更新 |
| app/webui/tests/client-viewport.test.ts | 新增 Chromium DOM 契约测试                                |
| app/webui/SKILL.md, app/webui/DESIGN.md | 布局契约同步                                              |
| CHANGELOG.md                            | Unreleased 片段（Fixed）                                  |

### 相关 Issue/PR

Issue #150；分支 `fix/webui-viewport-lock`（堆叠于 `refactor/webui-dead-code`，
因 theme.css/App.vue 同文件）。合并次序：#153（批次 1）之后。

### 影响范围简述

窄屏布局行为变化（页面整体滚动 → 列内滚动、composer 由 sticky 变常驻）——
维护者明确要求的重构方向；lg+ 三列行为不变（原本的列滚动此时才真正被锁住）。
文档层（SKILL/DESIGN）同步；用户可见面 README 无布局细节，无需改动。

### 测试与门禁

lint=pass typecheck=pass test=pass（含新增 client-viewport 契约测试：
三断点断言页面不可滚动、流列承接溢出、窄屏 composer 可见）
build=pass

### 设计决策

- 用 DOM 契约测试而非快照测试锁定不变量（页面高度 == 视口），测试里以纯
  DOM 追加行模拟长会话，避免与应用行为耦合。
- 删除窄屏 sticky composer：viewport-lock 后 sticky 处于非滚动容器中属于
  死规则，按 no-dead-code 规则一并清理，行为由 flex 布局自然达成。

---

AI-Generated: 批次 2 — webui 页面高度固定（body/#app viewport-lock + 窄屏滚动收敛到流列 + composer 常驻 + 布局契约文档同步 + Chromium 契约测试）
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: app/webui/client/theme.css, app/webui/client/App.vue, app/webui/tests/client-viewport.test.ts, app/webui/SKILL.md, app/webui/DESIGN.md, CHANGELOG.md, changelog/2026-09-05.md
AI-gate: lint=pass typecheck=pass test=pass (640) build=pass

---

## 批次 3 — webui 组件抽离：会话流 / 弹层切换 + 组件测试（refactor）

### 变更摘要

App.vue 原内联「内容 chat 栏」（用户气泡 + 四类卡片分发 + EmptyState +
两个自动滚动 watcher）与「弹层切换」（shortcuts/settings 开关 + Esc 关闭），
本次抽出为独立组件，App.vue 收敛为纯组合（script 128 行）：

- `components/ConversationStream.vue`：会话流列（无 props，状态来自
  usePlanFlow 单例）；自动滚动所依赖的流尺寸累积抽为纯函数
  `streamVolume()`（plan-flow.ts 内，node 可测）。
- `components/ModalLayer.vue` + `composables/ui-state.ts`：弹层切换状态机
  （模块单例，与 session/plan-flow 同模式）；Esc 走 `closeOverlays()`；
  快捷键主循环、斜杠命令、头部按钮翻转同一组 refs。
- 组件测试（依赖冻结，不引入 @vue/test-utils）：
  - `stream-volume.test.ts`（node）：streamVolume 纯函数 4 例 +
    useUiState 状态机 4 例（含模块单例语义）。
  - `client-components-dom.test.ts`（Chromium，无浏览器自动跳过）：
    ModalLayer 键盘契约（Ctrl+, → settings、? → shortcuts、一次 Esc 全关）、
    ConversationStream 全链路渲染（气泡 → Run plan → result）、自动滚动
    follow（流列滚自身、页面不动）。

### 变更类型：refactor

| 文件                                               | 变更                       |
| -------------------------------------------------- | -------------------------- |
| app/webui/client/components/ConversationStream.vue | 新增（自 App.vue 抽出）    |
| app/webui/client/components/ModalLayer.vue         | 新增                       |
| app/webui/client/composables/ui-state.ts           | 新增                       |
| app/webui/client/composables/plan-flow.ts          | 新增纯函数 streamVolume    |
| app/webui/client/App.vue                           | 收敛为纯组合（样式随迁）   |
| app/webui/tests/stream-volume.test.ts              | 新增（node，8 例）         |
| app/webui/tests/client-components-dom.test.ts      | 新增（Chromium，3 例）     |
| app/webui/SKILL.md                                 | 组件清单 + 测试行同步      |
| CHANGELOG.md                                       | Unreleased 片段（Changed） |

### 相关 Issue/PR

Issue #151；分支 `refactor/webui-extract-components`（堆叠于
`fix/webui-viewport-lock`）。合并次序：#154 之后。

### 影响范围简述

渲染输出零变化（既有 client 字节级快照不破裂为证）；键盘契约行为不变；
localStorage 键不变。SKILL.md 组件清单同步。新增测试 11 例。

### 测试与门禁

lint=pass typecheck=pass test=pass（640 → 651：+11 新例；快照全保留）
build=pass

### 设计决策

- streamVolume 放 plan-flow.ts（与 CardState 同处，类似 priorTurnsOf 的
  纯助手），不另设 lib 文件。
- 不引入 @vue/test-utils（运行时依赖冻结 + 技术选型冻结）：组件行为测试
  走既有 Chromium DOM 测试模式，纯逻辑走 node 单测——两层覆盖。

---

AI-Generated: 批次 3 — webui 组件抽离（ConversationStream + ModalLayer/ui-state，App.vue 纯组合化，新增 11 例组件测试，渲染零变化）
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: app/webui/client/components/{ConversationStream,ModalLayer}.vue, app/webui/client/composables/{ui-state.ts,plan-flow.ts}, app/webui/client/App.vue, app/webui/tests/{stream-volume,client-components-dom}.test.ts, app/webui/SKILL.md, CHANGELOG.md, changelog/2026-09-05.md
AI-gate: lint=pass typecheck=pass test=pass (651) build=pass

---

## 批次 4 — 设置界面 provider 配置：模型链接查询 + 仅粘贴 key + 防窥掩码（feat）

### 变更摘要

设置面板从「只读视图」升级为「provider 可配置」，实现维护者的三步要求
（选 provider → 粘贴 key → 保存）且**无需手输 URL**：

- **模型链接查询**：新增服务端 `app/webui/src/provider-catalog.ts`——每个
  注册 provider 的默认端点 + 取 key 控制台链接 + keyless 标记（与
  `packages/ai/src/providers/*` 的默认值对齐，头注释强制同步；测试与注册表
  `providerNames()` 做奇偶校验，新增 provider 漏配目录即挂门禁）。
  `GET /api/config` 下发 `providerCatalog`，客户端零硬编码。
- **写路径（受控、最小面）**：`POST /api/config/provider {provider, apiKey?,
baseUrl?, activate?}`——复用 `@tau/core` 的 `setConfigValue`（与 CLI
  `tau provider set-key` 同一通道，0600 权限）；Ollama 端点写 `host` 字段、
  其余写 `baseUrl`；`activate` 一键切换活跃 provider；keyless provider
  拒绝 key（镜像 CLI 的 KEYLESS 拒绝）；目录刷新 fire-and-forget（保存
  响应不被不可达端点拖住）；明文 key 只存在于本机请求体与 0600 配置文件，
  从不回显、从不进日志，响应一律标准脱敏 payload。**设计契约变更**：
  「NO write path」更新为「唯一可写切片」，gate/风险策略仍 GET-only。
- **防窥掩码（UI）**：`components/ProviderSetup.vue`——provider 芯片
  （含可用性 ✓/✕ 与 `·key` 标记）、端点自动带出（advanced 折叠项可改）、
  key 输入 `type=password` + 显/隐切换（显形 8s 自动重新掩码，防旁观窥屏）、
  保存成功即清空输入、已保存 key 仅以 `sk-***last4` 掩码回显。
- 设置面板布局原语（settings-section/row 等）提升至 theme.css 全局层
  （Vue scoped 样式不跨组件边界，ProviderSetup 是 SettingsPanel 子组件）。
- 保存后设置面板重拉配置 + `refreshStatus()`（头部芯片同步活跃 provider）。

### 变更类型：feat（含设计契约变更，已同步文档）

| 文件                                              | 变更                                                     |
| ------------------------------------------------- | -------------------------------------------------------- |
| app/webui/src/provider-catalog.ts                 | 新增（provider 目录 + baseUrlField 映射）                |
| app/webui/src/server.ts                           | GET /api/config 下发目录；新增 POST /api/config/provider |
| app/webui/client/lib/api.ts                       | ProviderCatalogEntry 类型 + ConfigPayload 扩展           |
| app/webui/client/components/ProviderSetup.vue     | 新增（选 provider → 粘贴 key → 防窥保存）                |
| app/webui/client/components/SettingsPanel.vue     | 挂载 ProviderSetup；徽章/页脚文案同步                    |
| app/webui/client/theme.css                        | 设置布局原语提升为全局类                                 |
| app/webui/tests/server-provider-config.test.ts    | 新增（14 例：正/负例/掩码/0600/奇偶校验）                |
| app/webui/tests/client-provider-setup-dom.test.ts | 新增（2 例 Chromium：全流程 + keyless）                  |
| README.md / README.zh-CN.md                       | `tau web` 行更新（双语）                                 |
| docs/guide/webui.md / docs/en/guide/webui.md      | 新增 Provider 配置小节（双语）                           |
| app/webui/SKILL.md / app/webui/DESIGN.md          | 组件清单 + 后端契约 + §6.12 同步                         |
| CHANGELOG.md                                      | Unreleased 片段（Added）                                 |

### 相关 Issue/PR

Issue #152；分支 `feat/webui-provider-setup`（堆叠于
`refactor/webui-extract-components`）。合并次序：#155 之后（单元栈末位）。

### 影响范围简述

- 用户可见面：设置面板新增 provider 配置区（本批次新能力）；README/docs
  双语同步。
- 契约变更（显式声明）：`GET /api/config`「NO write path」→「唯一写切片
  = POST /api/config/provider，仅 provider/apiKey/baseUrl/activate 四字段」；
  gate/风险策略与执行通道零变化，安全面不扩大（端点仅绑定 127.0.0.1）。
- 运行时依赖零新增。

### 测试与门禁

lint=pass typecheck=pass test=pass（651 → 667：+14 服务端例 +2 Chromium 例）
format=pass build=pass

### 设计决策

- 目录数据放服务端（`providerCatalog` 下发）而非客户端常量：单一来源 +
  奇偶校验测试防漂移；客户端保持 engine-agnostic。
- 写端点复用 `setConfigValue` 而非直写配置对象：与 CLI 完全同一条校验
  通道（字段白名单、apiKey 非空、0600），不引入第二套写逻辑。
- 目录刷新 fire-and-forget：CLI `set-key` 会 await 并打印目录，WebUI 的
  保存响应若 await 会被不可达端点拖住（实测超时），改为不等待、下一次
  GET /api/config 自然反映。
- 设置面板布局原语提升 theme.css：比在两个组件里重复 scoped 样式更符合
  设计系统的单一来源。

---

AI-Generated: 批次 4 — 设置界面 provider 配置（服务端 provider 目录下发 + POST /api/config/provider 受控写路径 + 防窥掩码 UI + 双语文档同步 + 16 例新测试）
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: app/webui/src/provider-catalog.ts, app/webui/src/server.ts, app/webui/client/lib/api.ts, app/webui/client/components/{ProviderSetup,SettingsPanel}.vue, app/webui/client/theme.css, app/webui/tests/{server-provider-config,client-provider-setup-dom}.test.ts, README.md, README.zh-CN.md, docs/guide/webui.md, docs/en/guide/webui.md, app/webui/SKILL.md, app/webui/DESIGN.md, CHANGELOG.md, changelog/2026-09-05.md
AI-gate: lint=pass typecheck=pass test=pass (667) format=pass build=pass

---

## 勘误 — 单元分支 CI 格式门禁返工（批次 1–4 分支栈重整）

### 事由

批次 1（#153）与批次 2（#154）分支的首次 CI 在 `format check (oxfmt)` 失败：
根因是提交前门禁只跑了 lint/typecheck/test/build，漏跑 `pnpm format:check`
（格式修正被批次 3 的全仓 `pnpm format` 顺带吸收，导致批次 3/4 分支绿而
批次 1/2 分支红）。按「CI 失败必须根因解决、不得绕过」处理：

- `changelog/2026-09-05.md` 的格式化差异用 `--amend` 归还批次 1 提交
  （72c56b5），U2 提交以 `rebase --onto` 重放后同样 amend（85a6ea5），
  U3/U4 随栈重放（28b89bf / 7fc5148）；四个分支 tip 均通过
  format:check + lint + typecheck，U4 tip 全量测试 667 通过。
- 教训固化：**AI-gate 尾注包含 format=pass 之前，必须实际运行
  `pnpm format:check`**（后续批次 3/4 的门禁记录已如实标注 format=pass，
  批次 1/2 的历史门禁数字保持原样不涂改，以此勘误承接）。

另：main 分支当前 CI 失败为「Dependency audit」步骤，早于本次工作
（2026-09-04 的 main 运行即失败），与本次四个 PR 无关——已在 #153 留言
说明，供维护者排查。

---

AI-Generated: 勘误 — 单元分支 CI 格式门禁返工记录（U1/U2 amend 格式修正 + U3/U4 重放，四分支 tip 全绿）
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: changelog/2026-09-05.md
AI-gate: lint=pass typecheck=pass test=pass (667) format=pass

---

## 批次 5 — 发布单元：v0.6.1 发布准备（回滚勘误后重建；总 PR 载体）

### 变更摘要

维护者指令以**一个总 PR** 收口本次 WebUI 复合需求，并勘误首个总 PR（#158，
已关闭）的版本层级错误：#158 曾把版本锁步 bump 到 0.7.0，与本批单元 PR
#156 文档既有标注（README 双语命令表与 docs/webui 均写明
"v0.6.1: settings provider setup"）矛盾——版本层级应遵循单元文档标注
（0.6.0 → **0.6.1**），不由总 PR 擅自改写。按维护者指令回滚 #158 与
收口 Issue #157、删除 `release/v0.7.0-webui-revamp` 分支后，以
`release/v0.6.1-webui-revamp` 重建：自 main 快进合入批次 1–4 四个单元
分支（72c56b5 → 85a6ea5 → 28b89bf → 46ed890，与各自 CI 绿的 tip 完全
一致），本批次为发布单元，随后总 PR（集成分支 → main）呈交人工合并。

- **版本锁步 bump**：14 个 package.json（root + docs + core/engine/markdown/
  plugins/skills/tools/ui/agent/ai + cli/webui/tui 三 app）0.6.0 → 0.6.1
  （sed 可重放；lockfile 零 diff——pnpm lockfile 不记录 importer 自身版本；
  运行时版本经 @tau/agent 的 createRequire 读取，自动跟随）。**工具注记**：
  `pnpm -r exec pnpm version` 在 pnpm 的 unclean-tree 守卫下不可用（首个包
  bump 即弄脏工作树，后续包全数拒绝）——与批次 10 结论一致，sed 是唯一
  可重放路径。
- **CHANGELOG.md**：Unreleased（批次 1–4 四个条目：provider 配置 feat、
  viewport 锁定 fix、组件抽离 refactor、死代码清理）归档为
  `## 0.6.1 — 2026-09-05`（含发布导语），按 §8.3 恢复空 Unreleased。
- **双语 README**：Tests 徽章 636 → 667（批次 1–4 新增 28 例后的全量数）。

### 刻意不做（维护者动作，见 AGENTS/release.md + §12）

- git tag v0.6.1、`pnpm -r pack` 家族冒烟、`pnpm publish -r`：需要 npm
  账号与发布决定，AI 从不代办。
- 单元 PR #153–#156 保持 open：待总 PR 人工合并后按 §3「total-PR
  reconciliation」逐一关闭（附 git 证据）。

### 门禁

- lint 0/0 ｜ typecheck ✅ ｜ test 667 全绿 ｜ format:check ✅ ｜
  webui build ✅ ｜ 全仓 build ✅ ｜ `tau --version` = 0.6.1

---

AI-Generated: 批次 5 — 发布单元重建（#158 版本层级勘误回滚后，14 包锁步 bump 0.6.0→0.6.1 + CHANGELOG Unreleased 归档 0.6.1—2026-09-05 + 空 Unreleased 复原 + 双语徽章 636→667）
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: 14x package.json, CHANGELOG.md, README.md, README.zh-CN.md, changelog/2026-09-05.md
AI-gate: lint=pass typecheck=pass test=pass (667) format=pass webui-build=pass workspace-build=pass

---

## 批次 6 — v0.6.1 运行截图入库（WebUI 新功能 ×6 PNG，随总 PR；批次 12 先例）

### 变更

- **截图 harness** `app/webui/scripts/shot-v061.mjs` 随图入库
  （`shots:v061` npm 入口；#158 回滚前的 `shot-v070.mjs` 更名并对齐
  版本层级）：对齐 `shot-v060.mjs` 约定——真实服务器（`startWebUi`）+
  `TAU_HOME` 沙箱（离线 mock，事后删除）+ 无头 Chromium 实机会话；
  输出相对脚本定位；本批次**全部场景真实端到端，零流拦截**（无需
  `page.route`）。
- **六张 PNG**（`app/webui/docs/screenshots/`，1440×900@2x 暗色，头部
  徽标 v0.6.1——发布单元批次 5 之后运行）：`provider-setup.png`（选
  deepseek → 端点由服务端目录带出 + 取 key 控制台链接）、
  `provider-setup-key.png`（粘贴后密码点掩码——隐私默认态）、
  `provider-setup-reveal.png`（显式窥视：明文 + hide，8s 自动重掩码）、
  `provider-setup-saved.png`（保存后 `sk-***last4` + `·key` 芯片 +
  输入清空）、`provider-setup-card.png`（设置区元素裁切）、
  `viewport-lock.png`（三请求长会话：流列内滚、第三卡被折裁切、
  composer 钉底——页面自身不生长）。
- **防污染细节**：deepseek 保存时 `activate` 复选框默认勾上（组件真实
  UX），会把活跃 provider 切到假 key 的 deepseek——harness 在截图序列
  末尾选回 mock 一键保存恢复，保证后续 plan 场景走离线 mock
  （`/api/plan/stream` 200）。
- **stdout 证据**：viewport 场景打印页面高度证据
  （`pageScrollHeight == innerHeight == 900` 且流列溢出 895 > 722），
  不满足即抛错——截图与 U2 契约测试互为印证。
- **文档同步**：`app/webui/README.md` Screenshots 节增三个 v0.6.1
  展示位 + More 行；`docs/screenshots/README.md` 表格增六行清单、
  Regenerate 增 `shots:v061` 命令、诚实注记更新为「三套 harness 全部
  真实端到端（唯一拦截场景仍是 v060 的 html-preview）」。

### 门禁

- format:check ✅（markdown 表格 oxfmt 通过）｜ webui build ✅ ｜
  test 667 全绿 ｜ harness 运行零错误、六图落盘（徽标 v0.6.1）

---

AI-Generated: 批次 6 — v0.6.1 WebUI 新功能截图入库（shot-v061.mjs harness + 6 PNG + 双层 README 同步 + npm 入口；#158 回滚后按 v0.6.1 重捕）
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: app/webui/scripts/shot-v061.mjs, app/webui/docs/screenshots/*, app/webui/README.md, app/webui/package.json, changelog/2026-09-05.md
AI-gate: format=pass webui-build=pass test=pass (667) shots=6-captured

---

## 批次 7 — total-PR reconciliation 清点记录（总 PR #160 合并后的当次收口）

### 事由

维护者已合并总 PR #160（merge commit 0079c8f，release/v0.6.1-webui-revamp →
main）与栈底单元 PR #153（merged）。按 collaboration.md §3「total-PR
reconciliation」，AI 立即执行 PR 账目清点，杜绝「总 PR 已合并而单元 PR
仍 open」的悬挂状态。

### 判定与证据（以 git 为准）

- **祖先关系**：`git merge-base --is-ancestor <head> main` —— 四个单元
  tip（72c56b5 / 85a6ea5 / 28b89bf / 46ed890）与集成 tip（b92a840）全部
  是 main（0079c8f）的祖先。
- **补丁等价**：`git cherry main <unit-ref>` —— #154/#155/#156 三个分支
  输出为空（全部补丁已在 main）。
- **树级一致**：main 树哈希 = 集成 tip 树哈希 = `71088d4`
  （`git diff origin/release/v0.6.1-webui-revamp main` 为空）——总 PR
  携带的内容与单元之和逐字节一致，零丢失。

### 清点结果

| 单元 PR               | 内容               | 判定                      | 处置               |
| --------------------- | ------------------ | ------------------------- | ------------------ |
| #153（U1，base=main） | 死代码清理         | 已由维护者合并（merged）  | 无需动作           |
| #154（U2）            | 页面高度固定       | 内容已在 main（证据如上） | 已关闭，附证据评论 |
| #155（U3）            | 组件抽离           | 内容已在 main（证据如上） | 已关闭，附证据评论 |
| #156（U4）            | 设置 provider 配置 | 内容已在 main（证据如上） | 已关闭，附证据评论 |

- 无「内容未进入 main」的单元，无需重落 PR。
- 分支按仓库惯例保留不删（历史 PR 分支均保留）。
- 已废弃的 #157（首个收口 Issue，随 #158 回滚）维持 open——AI 无关闭
  Issue 权限，废弃说明见 #158 留言，待维护者关闭。

### 后续（维护者动作，AGENTS/release.md + §12）

- git tag v0.6.1、`pnpm -r pack` 家族冒烟、`pnpm publish -r`：AI 从不代办。

---

AI-Generated: 批次 7 — total-PR reconciliation 清点记录（#154/#155/#156 关闭 + git 证据，#153/#160 已合并，树哈希 71088d4 一致）
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: changelog/2026-09-05.md
AI-gate: format=pass

---

## 批次 8 — 统一思考配置层（feat）

### 变更摘要

单元 U1（Issue #162，分解公示见 #162–#166）：把散落各 provider 的思考旋钮
（`think` / `thinking` / `thinkingBudget`）归一为统一配置层，作为 TUI（U2）/
WebUI（U3）选择组件的数据面。新增 `packages/ai/src/thinking.ts`：能力矩阵
（UI 据此渲染，不硬编码）、归一化读写（旧布尔与 ollama `think:true` 兼容读、
能力外旋钮拒绝、null 清除回默认）、effort→预算预设；五家 provider 接线
（全部 opt-in，未配置 = 请求体字节级不变）；CLI 新增
`tau provider thinking` 命令并在 `provider list` 行内携带状态。

### 变更类型：feat

| 变更文件                                 | 说明                                                                       |
| ---------------------------------------- | -------------------------------------------------------------------------- |
| `packages/ai/src/thinking.ts`            | 新增：能力矩阵 + get/setThinkingConfig + describeThinking + EFFORT_BUDGETS |
| `packages/ai/src/index.ts`               | barrel 导出 thinking 模块                                                  |
| `packages/core/src/config/store.ts`      | PROVIDER_FIELDS += thinkingEffort；注释更新归一化语义                      |
| `packages/ai/src/providers/anthropic.ts` | thinkingFragment 改经归一化读取（effort 预设/显式预算优先，legacy 不变）   |
| `packages/ai/src/providers/gemini.ts`    | off→0、on 无预算→动态 −1、effort→预设、显式预算优先                        |
| `packages/ai/src/providers/openai.ts`    | effort→reasoning_effort（仅显式时发送，chat+stream 两体）                  |
| `packages/ai/src/providers/deepseek.ts`  | mode→thinking:{type}（harness 体经 HarnessConnection.extraBody + 回退体）  |
| `packages/ai/src/providers/ollama.ts`    | mode→think:true/false（legacy think 兼容读）                               |
| `app/cli/src/provider.ts`                | `tau provider thinking [mode] [effort]` + provider list thinking 状态      |
| `packages/ai/tests/thinking.test.ts`     | 新增 25 例：矩阵/读写/兼容/预设/拒绝/五家线格式正负断言                    |
| `docs/guide/providers.md`(+en)           | 「思考模式与思考强度（v0.6.2）」节：键、能力矩阵表、CLI 示例               |
| `README.md` / `README.zh-CN.md`          | provider 节后三端一致的思考选择说明                                        |
| `CHANGELOG.md`                           | Unreleased → Added 片段                                                    |

### 相关 Issue/PR

Issue #162；分支 `feat/ai-thinking-layer`；PR #167（base=main，内容同时以
ff 并入集成分支 `release/v0.6.2-ai-selection`，v0.6.1 批次 1–4 先例）。
U2（#163）/U3（#164）依赖本单元。

### 影响范围简述

行为增量：新配置键 + 新 CLI 命令 + provider 线格式在显式配置时携带新字段。
零配置时全部请求字节级不变（线格式负断言锚定）。安全面零变化（engine 未触碰）。

### 测试与门禁

lint 0/0 ｜ typecheck ✅ ｜ test 692 全绿（基线 667 + 25）｜ format:check ✅ ｜
build ✅ ｜ 冒烟 `tau --version`=0.6.1、`tau provider thinking anthropic` 输出能力

### 设计决策

- effort→预算预设统一为 2048/8192/16384（anthropic/gemini 同表，显式预算永远优先）。
- openai 只做 effort（reasoning 模型无关闭语义）；deepseek 只做 mode（v4 统一模型
  的思考开关），强度不入不支持契约的端点。
- 拒绝而非静默忽略能力外写入——UI 渲染同一张矩阵，仅在直改配置文件时触达。

---

AI-Generated: 批次 8 — 统一思考配置层（thinking.ts 能力矩阵 + 五家 provider 线格式接线 + tau provider thinking 命令 + 25 测试）
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: packages/ai/src/thinking.ts, packages/ai/src/providers/*, packages/core/src/config/store.ts, app/cli/src/provider.ts, packages/ai/tests/thinking.test.ts, docs/guide/providers.md(+en), README×2, CHANGELOG.md
AI-gate: lint=pass typecheck=pass test=pass (692) format=pass build=pass

---

## 批次 9 — TUI 模型与思考选择（feat）

### 变更摘要

单元 U2（Issue #163，依赖批次 8）：TUI 三条命令进入共享目录——`/model`
（无参从目录选择：TTY 交互选择器经单读者安全的 overlay 接缝，非交互降级
列表永不挂起；带参直接设置并对目录外模型警告，与 CLI 语义一致）、
`/thinking`（展示状态与能力/带参写入，能力拒绝原因可见）、`/provider`
追加 thinking 状态行。选择服务（listModelCatalog / setProviderModel /
thinkingState / updateThinking）落在 `@tau/agent` session.ts——UI 无关
薄封装，TUI 不新增依赖边。U1 范围内去重：CLI describeState 并入
describeThinking。

### 变更类型：feat

| 变更文件                               | 说明                                                                                                                     |
| -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| `packages/agent/src/session.ts`        | 新增四个选择服务 + ThinkingState（含 summary）                                                                           |
| `packages/agent/src/commands.ts`       | 共享目录新增 /model、/thinking（tui 面）                                                                                 |
| `app/tui/src/index.ts`                 | 两命令处理器 + /provider 扩展；openPalette 的 overlay 舞步抽为 withOverlay 注入 overlayRunner 接缝；会话关闭时 seam 复位 |
| `packages/agent/tests/session.test.ts` | +5 例：目录服务、未知 provider、校验写、状态摘要、能力拒绝                                                               |
| `app/tui/tests/tui-commands.test.ts`   | +9 例：目录注册、显式设置+警告、非交互降级、缺目录解释、/thinking 读写与参数校验、/provider 扩展行                       |
| `app/tui/README.md`                    | 命令表增两行                                                                                                             |
| `docs/guide/tui.md`(+en)               | 设计要点新增「模型与思考选择（v0.6.2）」                                                                                 |
| `README.md` / `README.zh-CN.md`        | 修复 `tau provider use deepseek [model]` 笔误 + 目录驱动前端说明                                                         |
| `CHANGELOG.md`                         | Unreleased → Added 片段（TUI 单元）                                                                                      |

### 相关 Issue/PR

Issue #163；分支 `feat/tui-model-thinking`（堆叠于 `feat/ai-thinking-layer`）；
PR base = 前序单元分支（v0.6.1 #154–#156 堆叠先例）；内容 ff 并入集成分支。

### 影响范围简述

TUI 新增两命令 + 一行状态；共享目录扩两名（/help、调色板、Tab 补全自动带出）；
会话内 overlay 复用不产生第二读者。选择写入与 `tau provider use` 同一配置通道；
安全面零变化。

### 测试与门禁

lint 0/0 ｜ typecheck ✅ ｜ test 全绿（692+14=706）｜ format:check ✅（见批次门禁终值）

### 设计决策

- 交互选择器经 startTui 注入的 overlayRunner 接缝运行（readline pause +
  keypress 监听摘挂 + 恢复）——测试/非 TTY 下 seam 为 null，处理器走列表降级，
  单读者约束（#69）不破。
- mock 自带确定性目录（mock-chat/mock-reasoner），非交互降级用真实目录断言。

---

AI-Generated: 批次 9 — TUI /model 选择器 + /thinking 命令（@tau/agent 选择服务 + overlay 接缝 + 14 测试 + 文档）
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: packages/agent/src/{session,commands}.ts, app/tui/src/index.ts, packages/agent/tests/session.test.ts, app/tui/tests/tui-commands.test.ts, app/tui/README.md, docs/guide/tui.md(+en), README×2, CHANGELOG.md
AI-gate: lint=pass typecheck=pass test=pass format=pass

---

## 批次 10 — WebUI 模型与思考选择（feat）

### 变更摘要

单元 U3（Issue #164，依赖批次 8）：WebUI 与 TUI/CLI 同构的模型与思考选择。
服务端三端点：`GET /api/models`（目录只读，?provider= + ?refresh=1，未知
provider 400）、`POST /api/config/model`（模型选择写，校验先于变更，响应 =
标准脱敏 configPayload）、`POST /api/config/thinking`（思考写，经批次 8 归一
层——能力矩阵之外的旋钮被拒）；`GET /api/config` 载荷新增 `thinking` 块
（活动 provider 的状态 + 能力 + 摘要）。客户端：provider 节模型行升级为
目录驱动下拉（含刷新按钮），新增能力驱动的 thinking 控件（mode/effort
mini-picker；无旋钮的 provider 如实渲染「no thinking knobs」提示）。

### 变更类型：feat（设计契约微扩：写路径 = 凭据（#152）+ 模型/思考（本单元）——同属 per-provider 请求旋钮；gate/风险策略仍只读）

| 变更文件                                        | 说明                                                                                           |
| ----------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| `app/webui/src/server.ts`                       | modelsPayload/saveModelChoice/saveThinkingChoice + 三路由 + configPayload.thinking             |
| `app/webui/client/lib/api.ts`                   | ThinkingCapability/ThinkingStatePayload/ModelCatalogPayload 镜像类型；ConfigPayload.thinking   |
| `app/webui/client/components/SettingsPanel.vue` | 模型下拉 + thinking mini-picker（能力驱动）+ 样式；onMounted 补回 load()（修复：改写时曾遗漏） |
| `app/webui/tests/server-model-thinking.test.ts` | 新增 7 例：目录读、写路径 400 矩阵、能力拒绝、脱敏响应                                         |
| `app/webui/tests/client-selection-dom.test.ts`  | 新增 2 例（真实 Chromium）：模型下拉选择持久化、thinking 控件能力渲染+持久化                   |
| `app/webui/tests/e2e.snapshot.test.ts`          | /api/config 快照更新（新增 thinking 块）                                                       |
| `app/webui/SKILL.md` / `app/webui/DESIGN.md`    | 「唯一写路径」→「窄写面（凭据+模型+思考）」；能力驱动控件描述                                  |
| `docs/guide/webui.md`(+en)                      | 「模型与思考选择（v0.6.2）」节                                                                 |
| `CHANGELOG.md`                                  | Unreleased → Added 片段（WebUI 单元）                                                          |

### 相关 Issue/PR

Issue #164；分支 `feat/webui-model-thinking`（堆叠于 U2）；PR base = U2 分支；
内容 ff 并入集成分支。

### 影响范围简述

写面从「凭据」微扩至「凭据+模型+思考」（均为 per-provider 请求旋钮，同一
setConfigValue 通道；gate/风险策略/执行通道零变化）。快照契约更新（thinking
块）。无新增运行时依赖。

### 测试与门禁

lint 0/0 ｜ typecheck ✅ ｜ test 716 全绿（707 + 9：服务端 7 + DOM 2）｜
format:check ✅ ｜ webui build ✅

### 设计决策

- thinking 块描述活动 provider（面板视图即活动视图；写非活动 provider 时
  面板不移动——切换 provider 后如实反映）。
- 控件渲染完全由服务端能力表驱动，客户端零硬编码——UI 与配置层的单一契约
  在 /api/config。

---

AI-Generated: 批次 10 — WebUI 模型下拉 + thinking 控件（三端点 + 能力驱动渲染 + 9 测试 + 契约文档）
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: app/webui/src/server.ts, client/lib/api.ts, client/components/SettingsPanel.vue, tests/{server-model-thinking,client-selection-dom,e2e.snapshot}.*, SKILL.md, DESIGN.md, docs/guide/webui.md(+en), CHANGELOG.md
AI-gate: lint=pass typecheck=pass test=pass (716) format=pass webui-build=pass

---

## 批次 11 — v0.6.2 发布单元（release）

### 变更摘要

单元 U5（Issue #166）：U1–U3 全部并入集成分支后执行——全工作区 14 个
package.json 锁步 0.6.1 → 0.6.2（脚本显式写版本，可重放）；
`CHANGELOG.md` Unreleased 归档为 `## 0.6.2 — 2026-09-05`（发布导语 +
Added 三条目），恢复空 Unreleased；双语 README 测试徽章 667 → 716。

### 变更类型：release（版本层级；无行为变化）

| 变更文件                        | 说明                                                                      |
| ------------------------------- | ------------------------------------------------------------------------- |
| 14 × `package.json`             | 根 + docs + 3 app + 9 packages 锁步 0.6.2                                 |
| `CHANGELOG.md`                  | Unreleased → `## 0.6.2 — 2026-09-05`（导语 + Added×3）；恢复空 Unreleased |
| `README.md` / `README.zh-CN.md` | Tests 徽章 667 → 716                                                      |
| `changelog/2026-09-05.md`       | 本批次                                                                    |

### 相关 Issue/PR

Issue #166；提交直接落在集成分支 `release/v0.6.2-ai-selection`
（v0.6.1 批次 5 先例：发布单元随总 PR 携带，无独立单元 PR）。

### 影响范围简述

纯版本层级。`@tau/*` 家族同版发布语义不变（`pnpm publish` 重写
workspace:*/catalog: 时引用同版）。

### 测试与门禁

lint 0/0 ｜ typecheck ✅ ｜ test 716 全绿 ｜ format:check ✅ ｜ build ✅ ｜
冒烟 `tau --version` = 0.6.2、`tau skill list` 正常

### 刻意不做（维护者动作，AGENTS/release.md + §12）

- git tag v0.6.2、`pnpm -r pack` 家族冒烟、`pnpm publish -r`：需要 npm
  账号与发布决定，AI 从不代办。
- U4（#165）截图随后（徽标 0.6.2），随总 PR 携带。

---

AI-Generated: 批次 11 — v0.6.2 发布单元（14 包锁步 bump + CHANGELOG 0.6.2 归档 + 空 Unreleased 复原 + 双语徽章 667→716）
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: 14× package.json, CHANGELOG.md, README.md, README.zh-CN.md, changelog/2026-09-05.md
AI-gate: lint=pass typecheck=pass test=pass (716) format=pass build=pass smoke=0.6.2
