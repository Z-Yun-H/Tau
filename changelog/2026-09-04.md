# Changelog — 2026-09-04

## 变更摘要

维护者指令："根据规范浏览项目，先清除死代码"。单单元需求（refactor），
未触发复合分解。流程：通读 AGENTS.md + AGENTS/* 规范 → TypeScript 编译器
API 全仓静态分析（TS 语法级导出/导入图 + 动态 `import()` 人工复核 +
`.vue` SFC 引用扫描 + tests 引用核对）→ Issue #126 公示清理方案 →
分支 `refactor/dead-code-sweep` 清理 6 处零引用死代码 → 门禁全绿 →
PR 提交待人工合并。

## 批次 1 — 死代码清理（refactor）

### 变更摘要

对全仓做系统死代码扫描后，清除 6 处经交叉验证为**零引用**的导出符号
（src / tests / .vue / docs / SKILL.md / 动态 import 解构均已排除）：

1. `packages/ai/src/prompt.ts` — 删除 `toolNames()`（注释声称
   "Convenience for tests/docs"，但无任何测试/文档消费者）及其连带的
   `allTools` 未使用导入；
2. `packages/ai/src/prompt.ts` — 删除 `export type RawPlan`（未使用的
   `z.infer` 别名；`planSchema` 本体存活）；
3. `packages/ai/src/reflect.ts` — 删除 `export type RawReflectDecision`
   （同上；`reflectSchema` 本体存活）；
4. `packages/plugins/src/mcp.ts` — 删除 `resetMcpSdkCache()`（孤儿测试缝：
   消费它的测试已不存在；`cachedSdk` 缓存本体存活且被 `getMcpSdk` 使用）；
5. `app/webui/client/lib/api.ts` — 删除 `ExecuteResponse` 接口（过时的
   读模型镜像：流式执行路径未消费该类型，全仓零引用）；
6. `app/webui/src/index.ts` — 删除 barrel 转发
   `GoalRegistry` / `approvalTtlMs` / `DEFAULT_APPROVAL_TTL_MS`
   （零 barrel 消费者、不在 README "Public API" 清单；`goal.ts` 源导出
   保留——server.ts 内部消费、tests 直接从 `../src/goal.js` 引用）。

### 变更类型：refactor

| 变更文件                      | 说明                                           |
| ----------------------------- | ---------------------------------------------- |
| `packages/ai/src/prompt.ts`   | 删 `toolNames()` + `RawPlan` + `allTools` 导入 |
| `packages/ai/src/reflect.ts`  | 删 `RawReflectDecision`                        |
| `packages/plugins/src/mcp.ts` | 删 `resetMcpSdkCache()`                        |
| `app/webui/client/lib/api.ts` | 删 `ExecuteResponse`                           |
| `app/webui/src/index.ts`      | 删 goal 域 3 个 barrel 转发                    |
| `CHANGELOG.md`                | 新增 Unreleased / Removed 片段                 |
| `changelog/2026-09-04.md`     | 本文件                                         |

### 相关 Issue/PR

- Issue #126（`[REFACTOR] dead-code sweep`，清理方案公示）
- PR：`refactor/dead-code-sweep` → `main`（Closes #126）

### 影响范围简述

- 运行时行为零变化：删除项全部为零引用符号；门禁 533 测试全绿。
- 发布面变化：`@tau/ai` / `@tau/plugins` 经 `export *` barrel 原先公开的
  4 个符号（`toolNames` / `RawPlan` / `RawReflectDecision` /
  `resetMcpSdkCache`）从类型面移除，已在 CHANGELOG `Removed` 记录；
  `@tau/webui` 的 `GoalRegistry` / `approvalTtlMs` /
  `DEFAULT_APPROVAL_TTL_MS` barrel 转发同批移除（源包 `goal.ts` 导出不变）。
- 安全面：未触碰。`packages/engine/src/safety.ts` 的 deny list /
  `CAUTION_PATTERNS` 等按规范判定为"表面死代码但实为安全防御"，保留；
  AGENTS 黄金规则 1/2 无冲突。

### 测试与门禁

- `pnpm lint`：0 warnings, 0 errors（168 files, 85 rules）
- `pnpm typecheck`：通过
- `pnpm build`：通过（tsdown workspace + tui/webui vite）
- `pnpm test`：52 files / **533 tests 全绿**（新克隆首跑失败的 7 个 e2e
  为缺 dist 所致，`pnpm build` 后全绿——与本次改动无关的既有测试前置）
- `pnpm format:check`：通过（276 files）
- 冒烟：`tau --version`、`tau ask "find ts files" --yes`（mock provider
  全链路）、`tau-web` 启动 + `GET /api/status` 200 均正常

### 设计决策

- **已发布包成套公开 API 不清理**：`@tau/markdown` barrel 的
  `defaultAnsiTheme` / `AnsiOptions` 与 `renderToAnsi` 主题注入配套
  （doc-comment `{@link}` 互链），判为有意公开 API，保留。
- **公开函数的参数/返回类型不清理**：`@tau/webui` barrel 的
  `RunningWebUi` / `StartWebUiOptions` / `RequestListenerOptions` 是
  README 文档化公开函数（`startWebUi` / `createRequestListener`）的
  配套类型，保留。
- **"仅测试引用"的导出不清理**：`setDshLlmLoaderForTests` /
  `resetDshLlmCache` 等测试缝是有意设计。
- **~30 处 `INTERNAL-ONLY` 过度导出不在本批**：符号存活、仅导出关键字
  冗余，属风格重构且有 churn 风险，另立单元处理。
- **依赖清理不在本批**：规范 §4 依赖变更走独立 PR。
- 分析工具为一次性本地脚本（仓库外 TS5 API），未引入新 devDependency，
  不触及黄金规则 4 / 技术选型冻结。

---

AI-Generated: 全仓死代码扫描并清除 6 处零引用导出（@tau/ai toolNames/RawPlan/RawReflectDecision、@tau/plugins resetMcpSdkCache、webui ExecuteResponse 与 goal 域 barrel 转发）
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: packages/ai/src/prompt.ts, packages/ai/src/reflect.ts, packages/plugins/src/mcp.ts, app/webui/client/lib/api.ts, app/webui/src/index.ts（仅删零引用导出，行为零变化）
AI-gate: lint=pass typecheck=pass test=pass (533 tests) format=pass

## 补充记录 — CI 依赖审计步骤网络超时（勘误，不改动上方批次内容）

PR #127 的 CI（run 33833118348）在 "Dependency audit" 步骤连续 3 次
（attempt 1–3）失败，错误完全一致：

```
Error: ERR_PNPM_AUDIT_BAD_RESPONSE
  × Failed to request the audit endpoint
    (https://registry.npmjs.org/-/npm/v1/security/advisories/bulk)
  ╰─▶ operation timed out
```

- **根因**：GitHub Actions runner → npm registry 的瞬时网络故障（4 分钟+
  请求超时），非安全告警（真实 advisory 会输出漏洞表而非超时）。
- **与本批次无关的证据**：本提交未触碰 `pnpm-lock.yaml` 与任何
  `package.json`；审计之前的全部检查步骤（lint / format / typecheck /
  build / smoke / tests+cov，CI 上 531 passed | 2 skipped）三次全绿。
- **处置**：已重跑 3 次并停手（避免无限重试）；根因分析已作为评论留在
  PR #127；按规范不修改 CI 跳过审计（那才是绕过）。提请维护者：待 npm
  registry 连通性恢复后 re-run 该 workflow，或在合并前本地执行
  `pnpm audit --prod` 复核（共享同一 lockfile）。

AI-Generated: PR #127 CI 依赖审计步骤三次网络超时的根因分析与处置记录（勘误小节）
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: changelog/2026-09-04.md（仅追加勘误小节，无代码改动）
AI-gate: lint=pass typecheck=pass test=pass (533 tests) format=pass（本批复跑）

## 批次 2 — commit 作者身份归属规则（docs）

### 变更摘要

PR #127 复查发现其 commit 因 author 邮箱写为 `ai@users.noreply.github.com`，
被 GitHub「邮箱 ↔ 账号」归属逻辑匹配到恰好注册了用户名 `ai` 的无关老账号
（PR 页面 commit 栏显示陌生用户；commit 已当场 amend + force-push 修正为
`88868011+Z-Yun-H@users.noreply.github.com`）。为杜绝复发，把「commit 作者
身份必须归属维护者 GitHub 账号、禁止编造 `*@users.noreply.github.com` 邮箱」
写入规则本四处：

1. `AGENTS/release.md` — "Commit & PR workflow" 下新增小节
   "Commit author identity (GitHub attribution)"：author/committer 邮箱
   必须为维护者账号官方 noreply 地址；编造 `<name>@users.noreply.github.com`
   会被匹配到同名用户（记录 PR #127 事故先例）；AI 参与声明仅经 message
   尾注承载；push 前 `git log --format='%an <%ae> | %cn <%ce>' -1` 自检。
2. `AGENTS/collaboration.md` — §7 增补同款强制条款（中文规范体），
   English TL;DR 第 7 条镜像，附录"执行任务前自检清单"加一项。
3. `AGENTS.md` — pre-PR checklist 在 "AI-authored?" 条目后新增作者邮箱
   自检条目。
4. `.gitmessage` — 模板注释区新增 "Commit author identity" 提醒块
   （commit.template 每次提交可见）。

### 变更类型：docs

| 变更文件                  | 说明                                         |
| ------------------------- | -------------------------------------------- |
| `AGENTS/release.md`       | 新增 "Commit author identity" 小节           |
| `AGENTS/collaboration.md` | §7 条款 + TL;DR 第 7 条 + 附录自检清单各一处 |
| `AGENTS.md`               | pre-PR checklist 新增作者邮箱自检条目        |
| `.gitmessage`             | 模板注释区新增作者身份提醒块                 |
| `changelog/2026-09-04.md` | 本批次记录                                   |

### 相关 Issue/PR

- Issue #128（`[DOCS] 新增 commit 作者身份归属规则`，变更方案公示）
- PR：`docs/commit-author-identity` → `refactor/dead-code-sweep`
  （堆叠分支，共享本日 changelog 文件；**合并次序：先 #127 后本 PR**，
  Closes #128）

### 影响范围简述

- 行为/契约/文档（用户面）：零变化——纯内部治理规则，`CHANGELOG.md`
  不加 Unreleased 片段（§8.3 docs 类用户不可见条目豁免，特此说明）。
- AI 工作流面：此后所有 AI commit 的 author/committer 邮箱固定为
  `88868011+Z-Yun-H@users.noreply.github.com`（本地 git config 已同步
  设置），AI 参与痕迹仍经 `AI-Generated:` / `AI-declaration:` 尾注审计。
- 安全面：未触碰。

### 测试与门禁

- `pnpm lint`：0 warnings, 0 errors（168 files, 85 rules）
- `pnpm typecheck`：通过
- `pnpm test`：52 files / **533 tests 全绿**
- `pnpm format:check`：通过（277 files）
- 纯 Markdown/注释变更，无代码路径受影响；门禁实测数字见 PR 正文。

### 设计决策

- **不回溯重写历史**：PR #127 的错误归属 commit 已当场修正完毕；
  本规则只约束后续提交，避免无谓的历史改写 churn。
- **规则落点选择**：author 身份属"提交与发布工作流"，主条款放
  `AGENTS/release.md`（提交工作流的规范归属地），`AGENTS/collaboration.md`
  §7 以强制条款引用之——避免两处正文重复演化（单一定义 + 多处引用）。
- **不加 CI 强制校验**（如 GitHub Actions 校验 commit 邮箱域名）：收益
  有限（fine-grained PAT 本就归属维护者账号，PR 作者侧已有保障）且
  引入新的 CI 维护面，如需另立单元。
- `.gitmessage` 提醒块放在 AI 声明块之前：作者身份是"提交动作本身的
  元数据"，先于"提交内容声明"出现更符合检查顺序。

---

AI-Generated: 新增 commit 作者身份归属规则（禁止编造 *@users.noreply.github.com 邮箱，防 GitHub 误归属），落点 AGENTS/release.md、AGENTS/collaboration.md §7/TL;DR/自检清单、AGENTS.md checklist、.gitmessage
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: AGENTS/release.md, AGENTS/collaboration.md, AGENTS.md, .gitmessage, changelog/2026-09-04.md（纯规则文档变更，零代码改动）
AI-gate: lint=pass typecheck=pass test=pass (533 tests) format=pass

## 批次 3 — U1 共享斜杠命令注册表（refactor）

### 变更摘要

维护者复合需求（v0.6.0 UX & 对话能力）单元 1/8。TUI 的 10 个斜杠命令
此前硬编码在 `handleLine()` switch 中，WebUI 无命令概念。为让两个前门
共享「命令名/别名/描述/参数提示」元数据（U2 TUI 面板、U3 WebUI 菜单
的共同地基），按依赖方向规则在 `@tau/agent` 新增 UI 无关注册表
（`session.ts` 本就是「两个交互前门的单一事实来源」，且两 app 均已依赖
`@tau/agent`，零新依赖边）：

- `packages/agent/src/commands.ts`：`SlashCommandDef` 纯数据定义
  （name/aliases/description/argsHint/argsKind/surfaces）+
  `slashCommandCatalog()` / `slashCommandsFor(surface)` /
  `findSlashCommand(token, surface?)`（含别名、大小写不敏感）+
  `parseSlashInvocation(line)`（首 token 命令 + 余量 args）+
  `slashCommandUsage(def)`。
- 目录内容：TUI 既有 9 命令（/help /provider /skills /history /status
  /md /view /clear /exit，/quit 归并为别名）+ webui 侧 6 命令预告
  （/new /theme /plan /agent /tools /settings，U3 消费）。
- `app/tui/src/index.ts`：switch 改注册表驱动分发（`commandHandlers`
  按主名索引，别名经 findSlashCommand 解析）；/help 输出改为目录生成
  （列表永不分叉）；未知/畸形 `/` 行保持既有行为——落入自然语言意图
  管线；`handleLine` 导出供测试。

### 变更类型：refactor

| 变更文件                                | 说明                                     |
| --------------------------------------- | ---------------------------------------- |
| `packages/agent/src/commands.ts`        | 新增共享命令注册表（纯数据，零依赖）     |
| `packages/agent/src/index.ts`           | barrel 增补 commands 导出                |
| `app/tui/src/index.ts`                  | handleLine 注册表驱动化 + /help 目录生成 |
| `packages/agent/tests/commands.test.ts` | 注册表完整性/别名/surface 过滤/解析用例  |
| `app/tui/tests/tui-commands.test.ts`    | 分发等价性测试（TUI 首个测试文件）       |
| `CHANGELOG.md`                          | Unreleased / Changed 片段                |
| `changelog/2026-09-04.md`               | 本批次                                   |

### 相关 Issue/PR

- Issue #131（U1 单元）；总分解：#130
- PR：`refactor/slash-command-registry` → `release/v0.6.0-ux-slash`
  （Closes #131）

### 影响范围简述

- 命令行为零变化（别名解析、未知命令落意图管线均保持）；/help 文案
  由目录生成，列宽统一（13 列对齐），描述措辞微调（/history 改为
  "recent history entries" 以适配双面语义）——CHANGELOG Changed 已记录。
- 依赖图零变化：`@tau/agent` 零新增 import；无新依赖。
- 后续单元依赖：U2（TUI 面板）与 U3（WebUI 菜单）直接消费本注册表。

### 测试与门禁

- `pnpm lint`：0 warnings, 0 errors（171 files, 85 rules）
- `pnpm typecheck`：通过
- `pnpm test`：54 files / **559 tests 全绿**（较上批 +2 files / +26 tests）
- `pnpm format:check`：通过（280 files）

### 设计决策

- **注册表只放元数据、不放执行**：执行语义留在各面（TUI→session 服务
  - ANSI，WebUI→客户端动作），避免 `@tau/agent` 反向感知 UI 细节。
- **归属 `@tau/agent` 而非 `@tau/core`**：命令解析指向的服务
  （getActiveProvider/listSkillSummaries/readRecentHistory/getSessionInfo）
  就在 agent.session；core 是零依赖领域词表，不宜承载。
- **webui 命令预告进目录**：surfaces 标注让 U3 直接消费同一词表，
  也让完整性测试（别名不冲突等）提前覆盖将来命令。
- 修复过程中发现并固化了 `parseSlashInvocation` 对大写命令（/EXIT）的
  解析语义（首版 indexOf 实现在大小写混合时 args 计算错误，已改为
  单遍正则 + 用例锁定）。

---

AI-Generated: 抽离共享斜杠命令注册表至 @tau/agent（SlashCommandDef 纯数据 + 目录/过滤/别名/解析 API），TUI handleLine 改注册表驱动分发、/help 目录生成，新增 26 个测试
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: packages/agent/src/commands.ts, packages/agent/src/index.ts, app/tui/src/index.ts, packages/agent/tests/commands.test.ts, app/tui/tests/tui-commands.test.ts, CHANGELOG.md, changelog/2026-09-04.md（命令行为零变化）
AI-gate: lint=pass typecheck=pass test=pass (559 tests) format=pass

## 批次 4 — U2 TUI `/` 实时快捷提示面板（feat）

### 变更摘要

维护者复合需求单元 2/8（Issue #132，依赖 U1 注册表）。TUI 在空行输入
`/` 立即弹出可过滤命令面板，继续输入缩小范围，`↑`/`↓`（`Ctrl+P/N`）
移动，`Tab`/`Enter` 插入，`Esc`/`Ctrl+C` 关闭；退格删掉 `/` 时面板关闭
并还原空提示符。面板之外 `Tab` 仍走 readline 原生补全（同一目录）。

- `packages/ui/src/suggest.ts` 新原语 `suggestFromList()`：raw-mode 键盘
  - 纯 ANSI 重绘 + 可注入流 + 非 TTY 立即降级（机制完全对齐
    picker.ts）；过滤时保持光标所在项不跳；视口默认 8 行滑动窗口；
    面板行严格从提示符行的下一行开始绘制（`\x1b[2K` 永不触及提示符行）。
- `app/tui/src/index.ts`：**单读取器约束下的接管方案**——空行 `/` 触发
  时 `rl.pause()` + 摘下 readline 的 keypress 监听（EventEmitter 公开
  API 捕获/还原），stdin 保持 raw 并 `resume()` 供面板读取；结束后按序
  还原监听与流，缓冲区同步（选中追加/退格移除/过滤同步）在
  `rl.resume()` 之后执行（paused 状态下 write 可能被丢弃）。创建接口时
  注入 `completer`（目录驱动的命令名 Tab 补全）。
- 文档：app/tui/README.md 斜杠表修正（补 /md /view /exit，此前过时）+
  面板小节；docs/guide/tui.md 设计要点增补。
- pty 真机冒烟（script + term-svg 管线）：`/pro` → 过滤 1/1 → 选中 →
  执行输出 provider 信息 → Ctrl+D 干净退出（exit code 0）。

### 变更类型：feat

| 变更文件                               | 说明                                  |
| -------------------------------------- | ------------------------------------- |
| `packages/ui/src/suggest.ts`           | 新增 suggestFromList 面板原语         |
| `packages/ui/src/index.ts`             | barrel 增补导出                       |
| `packages/ui/tests/ui-suggest.test.ts` | 13 个键击驱动用例（FakeTTY 先例复用） |
| `app/tui/src/index.ts`                 | 面板接管接线 + completer 注入         |
| `app/tui/README.md`                    | 斜杠表修正 + 面板小节                 |
| `docs/guide/tui.md`                    | 设计要点增补面板说明                  |
| `CHANGELOG.md`                         | Unreleased / Added 片段               |
| `changelog/2026-09-04.md`              | 本批次                                |

### 相关 Issue/PR

- Issue #132（U2 单元）；总分解 #130；依赖 U1（#131，已并入集成分支）
- PR：`feat/tui-slash-palette` → `release/v0.6.0-ux-slash`（Closes #132）

### 影响范围简述

- 用户可见：TUI 输入体验（核心诉求）；`@tau/ui` 公开 API 增
  `suggestFromList` / `SuggestItem` / `SuggestResult` / `SuggestOptions`。
- 安全面零变化；无新依赖（chalk 既有）；WebUI/引擎不受影响。
- 单读取器保证保持：面板期间 readline 暂停且无第二个 reader，
  确认挂起（answerSlot）与计划执行（draining）期间不触发。

### 测试与门禁

- `pnpm lint`：0 warnings, 0 errors（173 files, 85 rules）
- `pnpm typecheck`：通过
- `pnpm test`：55 files / **572 tests 全绿**（+13 suggest 用例）
- `pnpm format:check`：通过
- pty 冒烟：选中/过滤/执行/退出全链路通过（真实 script pty 捕获）

### 设计决策

- **接管而非并行**：live 面板需逐键响应，方案对比（completer-only、
  被动渲染条、`_ttyWrite` 内部 API 摘除、监听摘除+暂停接管）后选择
  最后者——只用公开 API、复用 picker 机制、可注入流可测。
- **rl.pause() 后必须 stdin.resume()**：pause 会连底层流一起暂停
  （pty 冒烟发现面板收不到击键的根因）。
- **退格删 `/` 即关闭**：缓冲区与面板过滤态同步的语义边界（dismiss
  时把最终 filter 同步回 readline 缓冲区）。
- Ctrl+D 在面板内为 dismiss（防止 EOF 语义在 raw 态面板中悬死）。

---

AI-Generated: TUI 输入 / 实时命令面板（@tau/ui suggestFromList raw-mode 原语 + 单读取器兼容接管 + readline completer 注入），README 斜杠表修正与 guide 增补
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: packages/ui/src/suggest.ts, packages/ui/src/index.ts, packages/ui/tests/ui-suggest.test.ts, app/tui/src/index.ts, app/tui/README.md, docs/guide/tui.md, CHANGELOG.md, changelog/2026-09-04.md
AI-gate: lint=pass typecheck=pass test=pass (572 tests) format=pass + pty 冒烟通过

## 批次 5 — U3 WebUI Composer `/` 快捷菜单（feat）

### 变更摘要

维护者复合需求单元 3/8（Issue #133，依赖 U1 注册表）。Composer 输入
`/`（裸命令 token，如 `/`、`/th`）浮出命令菜单：↑↓ 移动、Tab/Enter 执行、
Esc 关闭、鼠标悬停/点击可选；命令在**客户端本地执行**（从不作为意图
发给 AI），执行后清空输入框。

- **服务端** `GET /api/commands`：返回 `slashCommandsFor("webui")` 共享
  目录（纯元数据；与 TUI 面板同源，webui surface 过滤）。additive 路由，
  冻结 API 零改动。
- **客户端** `lib/slash.ts`（纯函数层）：`filterCommands`（前缀过滤 +
  动作解析）、`menuOpenFor`（裸 token 判定，空格即隐藏）、`clampIndex`；
  `SlashActionId` 动作表（new/theme/plan/agent/tools/settings/help/
  status/skills/history）。
- `Composer.vue`：浮层菜单（`.tau-surface` + 主题变量、reduced-motion
  降级、role=listbox/option 无障碍属性；浮于 beam 之上、不受 shell
  overflow 裁剪）；键盘拦截仅在菜单可见时生效。
- `App.vue`：`onSlashCommand` 动作执行（new→createThread、theme→
  cycleTheme、plan/agent→模式切换、help→shortcuts、settings→settings、
  status/skills/tools/history→打开 reference rail）；onMounted 增
  refreshCommands。
- 提示行与 ShortcutsModal 同步增补 `/` 行（键位三处同步规则）。

### 变更类型：feat

| 变更文件                                         | 说明                               |
| ------------------------------------------------ | ---------------------------------- |
| `app/webui/src/server.ts`                        | GET /api/commands（additive 路由） |
| `app/webui/client/lib/api.ts`                    | CommandInfo / CommandsPayload 类型 |
| `app/webui/client/lib/slash.ts`                  | 菜单纯函数层（过滤/动作/开合判定） |
| `app/webui/client/composables/session.ts`        | commands ref + refreshCommands     |
| `app/webui/client/components/Composer.vue`       | 浮层菜单 + 键盘拦截 + 提示行       |
| `app/webui/client/App.vue`                       | 动作执行接线 + commands 注入       |
| `app/webui/client/components/ShortcutsModal.vue` | `/` 快捷键行                       |
| `app/webui/tests/slash.test.ts`                  | 菜单纯函数用例                     |
| `app/webui/tests/server.test.ts`                 | /api/commands 路由用例             |
| `CHANGELOG.md` / `changelog/2026-09-04.md`       | 片段 + 本批次                      |

### 相关 Issue/PR

- Issue #133（U3 单元）；总分解 #130；依赖 U1（#131）
- PR：`feat/webui-slash-menu` → `release/v0.6.0-ux-slash`（Closes #133）

### 影响范围简述

- 用户可见：Composer `/` 菜单（核心诉求）；API additive 新路由
  （冻结契约零改动——既有快照/事件词表不变）。
- 快照选择器（textarea / Run plan / file.find in）未动；无新依赖；
  客户端仍零运行时 @tau/agent 引用（目录经服务端路由供给）。
- 安全面：菜单动作为纯客户端 UI 操作（新会话/主题/模式/面板开合），
  不触达执行通道。

### 测试与门禁

- `pnpm lint`：0 warnings, 0 errors（175 files, 85 rules）
- `pnpm typecheck`：通过
- `pnpm test`：56 files / **583 tests 全绿**（+11：slash 纯函数 8 +
  路由 1 + 收尾计数）
- `pnpm format:check`：通过；`pnpm --filter @tau/webui build`：通过

### 设计决策

- **目录经服务端路由而非客户端镜像**：客户端 bundle 不能拖入
  @tau/agent（node 内建依赖链），服务端单源供给保持「一份词表」承诺，
  客户端仍可对本地动作表做纯函数测试。
- **裸 token 即菜单、空格即隐藏**：webui 命令全为无参动作，语义边界
  简单可预期（与 U1 argsKind 字段衔接，将来加参命令时此处已留缝）。
- status/skills/tools/history v1 映射为「打开 reference rail」而非
  直切 tab——SidePanel 自持 tabs，不加 prop 钻孔，先交付价值。

---

AI-Generated: WebUI Composer / 快捷菜单（GET /api/commands 共享目录路由 + lib/slash.ts 纯函数层 + Composer 浮层菜单 + App 动作执行 + 键位三处同步），11 个新测试
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: app/webui/src/server.ts, app/webui/client/lib/{api,slash}.ts, app/webui/client/composables/session.ts, app/webui/client/components/{Composer,ShortcutsModal}.vue, app/webui/client/App.vue, app/webui/tests/{slash,server}.test.ts, CHANGELOG.md, changelog/2026-09-04.md
AI-gate: lint=pass typecheck=pass test=pass (583 tests) format=pass webui-build=pass

## 批次 6 — U4 增强对话模式（feat）

### 变更摘要

维护者复合需求单元 4/8（Issue #134）。此前每个请求只带 `{intent}`，
provider 看不到本会话的历史轮次——「对话」名不副实。本单元把对话
上下文贯穿全链路，且 **provider 层零改动**：

- `@tau/core`：新增 `PriorTurn` 类型（role + text）。
- `@tau/ai` `planningContext(intent, skillCatalog, priorTurns?)`：把
  prior turns 渲染为 `<conversation>` 块折叠进 `ctx.intent`——所有
  provider 的 user 消息就是 `ctx.intent` 原文，单点注入全 provider
  受益；**无轮次时输出与此前逐字节一致**（测试锁定）。
- `@tau/agent`：`PlanIntentOptions` / `RunGoalOptions` 增
  `priorTurns`（round-1 规划 + reflect 上下文快照同享）。
- 服务端：`readPriorTurns()` 消毒（最近 12 轮、role 白名单、文本
  ≤4000 字符、坏条目跳过不致命），`/api/plan/stream` 与
  `/api/goal/stream` 接受可选 `history` 字段（additive）。
- 客户端：`priorTurnsOf()` 从当前线程卡片提取（用户消息 + ok 的
  result/goal 最终输出，12 轮/2000 字符上限），plan 与 agent 提交
  均随请求携带。

### 变更类型：feat

| 变更文件                                         | 说明                                       |
| ------------------------------------------------ | ------------------------------------------ |
| `packages/core/src/types.ts`                     | PriorTurn 类型                             |
| `packages/ai/src/prompt.ts`                      | planningContext 第 3 参 + renderPriorTurns |
| `packages/agent/src/pipeline.ts`                 | PlanIntentOptions.priorTurns 透传          |
| `packages/agent/src/loop.ts`                     | RunGoalOptions.priorTurns（3 处接缝）      |
| `app/webui/src/server.ts`                        | readPriorTurns 消毒 + 2 路由接线           |
| `app/webui/client/lib/api.ts`                    | TurnInfo 读模型类型                        |
| `app/webui/client/composables/plan-flow.ts`      | priorTurnsOf + 2 提交载荷                  |
| `packages/ai/tests/prompt-prior-turns.test.ts`   | 渲染语义 4 用例（含逐字节回归锁定）        |
| `app/webui/tests/server-plan-stream.test.ts`     | 捕获式 provider 验证透传 2 用例            |
| `app/webui/tests/client-snapshot.test.ts` + snap | data-v 哈希规范化（见设计决策）            |
| `CHANGELOG.md` / `changelog/2026-09-04.md`       | 片段 + 本批次                              |

### 相关 Issue/PR

- Issue #134（U4 单元）；总分解 #130
- PR：`feat/conversation-mode` → `release/v0.6.0-ux-slash`（Closes #134）

### 影响范围简述

- 用户可见：同一会话内 AI 能看到先前轮次（plan 与 agent 模式皆然）；
  无历史时行为与此前完全一致（无 priorTurns 的请求逐字节不变）。
- API：additive 请求字段 `history`（响应/事件词表零变化）。
- reflect 循环经 ctxBase 快照同享对话上下文；历史不落盘（线程仍在
  客户端），服务端不存储任何会话状态。

### 测试与门禁

- `pnpm lint`：0 warnings, 0 errors（176 files, 85 rules）
- `pnpm typecheck`：通过
- `pnpm test`：57 files / **589 tests 全绿**（+6：prompt 渲染 4 +
  服务端透传 2）
- `pnpm format:check`：通过；`pnpm --filter @tau/webui build`：通过
- client-snapshot 2 快照经 data-v 规范化后重生成（结构逐字节保持）

### 设计决策

- **单点折叠而非 provider 各自渲染**：`ctx.intent` 就是各 provider 的
  user 消息——在 planningContext 折叠 = 7 个 provider + mock 零改动
  获得 conversational 能力；改 PlanningContext 类型则要动每个报文
  构造点（且 mock/deepseek/zai 无视觉等能力差异会放大复杂度）。
- **无历史 = 逐字节一致**：渲染层以测试锁定无轮次时的输出不变，
  确保对既有 provider 测试与行为零扰动。
- **双重上限（客户端 2000 字符/服务端 4000）**：服务端是硬上限，
  客户端提前瘦身减 payload。
- **client-snapshot 增加 data-v 规范化**：SFC 作用域哈希随组件源码
  变化，属于构建元数据而非结构——规范化后快照只锁结构，消除未来
  无意义的快照 churn（本轮重生成仅哈希行变化）。

---

AI-Generated: 对话模式 priorTurns 贯穿（core PriorTurn 类型 + prompt 层单点折叠 + agent/server/客户端透传与消毒，provider 层零改动，无历史时逐字节不变），6 个新测试 + 快照 data-v 规范化
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: packages/core/src/types.ts, packages/ai/src/prompt.ts, packages/agent/src/{pipeline,loop}.ts, app/webui/src/server.ts, app/webui/client/lib/api.ts, app/webui/client/composables/plan-flow.ts, packages/ai/tests/prompt-prior-turns.test.ts, app/webui/tests/{server-plan-stream,client-snapshot}.test.ts + snap, CHANGELOG.md, changelog/2026-09-04.md
AI-gate: lint=pass typecheck=pass test=pass (589 tests) format=pass webui-build=pass

## 批次 7 — U5 图片解析模块（issue #135，PR #142）

### 变更

- **core**：`ImageAttachment` 类型（kind/name/mediaType/dataBase64）+
  `PlanningContext.attachments?` + `AIProvider.supportsVision?` 能力位。
- **prompt 层单点注记**：`planningContext()` 折叠 `<attachments>` 文本注记
  进 intent（视觉 provider 写 "attached"、纯文本 provider 诚实写
  "image was dropped"——由解析后的 provider 能力位决定措辞）；无附件时
  输出逐字节不变（测试锁定）。
- **四个视觉 provider 报文映射**：openai → `image_url` data URL 内容数组；
  anthropic → base64 image source block；gemini → `inline_data` part；
  ollama → 用户消息 `images` 字段（裸 base64）。无附件时全部保持历史
  报文形状；deepseek/zai/mock 零改动（注记即诚实降级）。
- **agent 层贯穿**：`PlanIntentOptions.attachments`（preparePlanning 先解
  析 provider 再建 ctx，注记措辞依赖 supportsVision）+ `RunGoalOptions
.attachments`（仅 round-1 携带——反思轮不重附图片，快照上下文保持
  attachment-free）。
- **server**：`readAttachments()` 消毒器（kind 白名单 / mediaType 白名单
  png-jpeg-webp-gif / ≤4 张 / 单张 base64 ≤5.6MB / 总量 ≤22MB / 严格
  base64 校验 / 复用 @tau/ui `probeImageHeader` 魔数门禁——声明类型必须
  与头部探测一致，改名文本文件无法冒充图片）；违规一律 400 明确报错且
  在流启动前；`/api/plan`、`/api/plan/stream`、`/api/goal/stream` 三端点
  贯穿；`BODY_CAP_BYTES` 1MiB→24MiB（附件自带更严单独上限）。
- **client**：`lib/attachments.ts` 纯函数层（chunked btoa、File API 校验、
  extension 回退、all-or-nothing 批次语义、payload/meta 投影）；
  Composer 附着按钮 + 粘贴 + 拖拽（drag-depth 防闪烁）→ 可移除预览
  chips（AttachmentChips 组件复用）；图片无文本发送使用显式默认意图；
  用户卡片显示附件 meta chips（thumb 会话级、persist 剥离——localStorage
  不存图片负载）；NDJSON 事件词表不变。

### 测试与门禁

- `pnpm lint`：0 warnings, 0 errors（181 files, 85 rules）
- `pnpm typecheck`：通过
- `pnpm test`：60 files / **620 tests 全绿**（+31：provider 报文 11 +
  server 校验 10 + client 纯函数 10）
- `pnpm format:check`：通过；`pnpm --filter @tau/webui build`：通过
- client-snapshot 全部保持（composer 新增节点不在快照锁定的流列区）

### 设计决策

- **能力位 + 单点注记**：支持视觉与否是 provider 的事实，注记措辞在
  provider 解析后才能定——preparePlanning 调序（解析在前）换取注记诚实，
  文本-only provider 不需要改一行代码即完成降级。
- **魔数门禁在 server、类型校验在两侧**：客户端校验为了快速可读的
  UX 报错，服务端校验才是安全边界；@tau/ui 的头部解析器无新依赖边
  （app 层依赖，audit 记录保持干净）。
- **负载不进卡片/事件/localStorage**：payload 只随请求走一次；卡片存
  meta（名称/类型/大小），缩略图 data URL 仅会话内存——quota 与隐私
  双重考量，重载后 chip 诚实降级为类型标签。

---

AI-Generated: 图片解析模块全链路（core 附件类型 + prompt 单点注记 + 四视觉 provider 报文映射与纯文本诚实降级 + agent/server 贯穿与魔数门禁 + Composer 附着/粘贴/拖拽 UX 与卡片 chips），31 个新测试
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: packages/core/src/types.ts, packages/ai/src/prompt.ts, packages/ai/src/providers/{openai,anthropic,gemini,ollama}.ts, packages/agent/src/{pipeline,loop}.ts, app/webui/src/server.ts, app/webui/client/lib/attachments.ts, app/webui/client/components/{Composer.vue,AttachmentChips.vue}, app/webui/client/App.vue, app/webui/client/composables/plan-flow.ts, packages/ai/tests/attachments.test.ts, app/webui/tests/{server-attachments,client-attachments}.test.ts, app/webui/package.json, CHANGELOG.md, changelog/2026-09-04.md
AI-gate: lint=pass typecheck=pass test=pass (620 tests) format=pass webui-build=pass

## 批次 8 — U6 生成内容显示优化（issue #136，PR #143）

### 变更

- **沙箱 HTML 预览**：ResultCard/GoalCard 输出中的 html 代码块获得
  「preview/code」切换——`<iframe sandbox="allow-scripts" srcdoc=…>`
  （不加 allow-same-origin，不透明源天然与父页隔离）；lib/preview.ts
  纯逻辑（srcdoc 构建：完整文档逐字直传、片段套中性壳）+ DOM 附着层
  （幂等、流式重渲染安全、先于 shiki 附着使高亮升级在外壳内原位完成）；
  escape-first markdown 契约不动。
- **只读文件预览路由 `GET /api/file?path=…`**：复用写工具同一套遏制定义
  （escapesWorkspace / isSystemWritePath）+ realpath 二次校验（符号链接
  穿越封堵）；保守 mime 白名单（pdf/图片/纯文本——html/js/svg/css 永不
  服务）；8MB 上限（413）；403/404 语义明确；nosniff + inline
  disposition + no-store。
- **ToolCallCard 二元查看器**：file.read 的 pdf → `<embed>` 浏览器原生
  查看器、图片 → `<img>`，后缀嗅探与服务端白名单奇偶锁定（测试）；
  失败回退 open ↗ 新窗引导；文本类型维持 shiki 现状（快照逐字节未动）。
- **快照规范化补全**：SFC 作用域还注入 `style="--vHASH"`（入场延迟），
  与 data-v 同源——一并归一化，消除未来的无意义 churn。

### 测试与门禁

- `pnpm lint`：0 warnings, 0 errors（184 files）
- `pnpm typecheck`：通过
- `pnpm test`：62 files / **636 tests 全绿**（+16：/api/file 路由 9 +
  client 预览纯逻辑 7）；快照重生成仅结构变化（user-col 包裹层 + --vHASH
  归一化）
- `pnpm format:check`：通过；`pnpm --filter @tau/webui build`：通过

### 设计决策

- **双通道而非放松**：HTML 预览走独立沙箱通道（iframe sandbox），渲染
  markdown 管线保持 escape-first——「能看」不等于「管线放宽」。
- **遏制复用 + realpath**：/api/file 与写工具共享同一遏制真相（单一
  定义、多执行点），realpath 重检封住符号链接这一名称遏制漏洞。
- **无新依赖**：pdf.js 不引入，浏览器原生 embed 已满足「依托浏览器
  能力」；嗅探仅后缀且两端奇偶锁定，不存在客户端误判路径。

---

AI-Generated: 生成内容显示优化（沙箱 HTML 预览通道 + /api/file 只读路由与遏制复用 + ToolCallCard 原生 PDF/图片查看 + 快照 --vHASH 归一化），16 个新测试
AI-declaration: this commit was authored by an AI agent under human direction.
AI-agent: Super Z (GLM)
AI-scope: app/webui/src/server.ts, app/webui/client/lib/preview.ts, app/webui/client/components/{ResultCard,GoalCard,ToolCallCard}.vue, app/webui/client/theme.css, app/webui/tests/{server-file,client-preview,client-snapshot}.test.ts + snap, CHANGELOG.md, changelog/2026-09-04.md
AI-gate: lint=pass typecheck=pass test=pass (636 tests) format=pass webui-build=pass
