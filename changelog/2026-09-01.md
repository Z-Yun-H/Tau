# Changelog — 2026-09-01

## 变更摘要

按维护者发布的《AI 助手代码协作通用操作规范（总要求·更新版）》，将 AI 协作规范从 2026-08 版（v1）同步为 2026-09 更新版（v2）：新增每日 changelog 文件夹制度、按变更类型区分的 Issue/PR 流程、技术选型约束、死代码与硬编码清理、测试质量保障专章与任务前自检清单，并同步更新了受影响的 dev-workflow 技能说明。

## 变更类型：docs（流程/规范文档）

| 文件                                  | 变更                                                                                                                                                                                                                                  |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `AGENTS/collaboration.md`             | 转录更新版规范为 v2：13 节 + 自检清单（原 11 节）；新增 §2 技术选型与架构约束、§3 按变更类型区分流程、§10 死代码与硬编码清理、§11 测试与质量保障；§8 重写为每日 `changelog/` 文件夹制度；§0 新增适用范围与强制力说明；英文 TL;DR 同步 |
| `AGENTS.md`                           | 黄金规则 7 扩展（PR body 报告门禁结果）、新增黄金规则 8（每日 changelog）；Change checklist 新增 6 项；Commit style 增加按变更类型的流程说明；AGENTS 索引表更新 collaboration.md 描述                                                 |
| `.claude/skills/tau-release/SKILL.md` | Changelog 步骤接入每日 `changelog/` 文件提炼；新增"Daily changelog duty"段                                                                                                                                                            |
| `.claude/skills/tau-test/SKILL.md`    | 新增 PR body 报告测试结果与 CI 失败根因化要求                                                                                                                                                                                         |
| `changelog/2026-09-01.md`             | 新建（每日 changelog 制度自本日起生效，本文件即首份）                                                                                                                                                                                 |
| `CHANGELOG.md`                        | Unreleased 段新增 Added 条目                                                                                                                                                                                                          |

## 相关 Issue/PR

- Issue: #31
- PR: #32（`Closes #31`，等待人工审核合并）

## 影响范围简述

- 仅影响 AI 协作流程文档与 dev-workflow 技能说明，**不触及任何运行时代码**（`packages/`、`app/` 源码零改动）。
- 安全模型（`docs/safety.md`、`packages/engine/src/safety.ts`）无变化。
- 双语 README 无需更新：本变更为内部协作流程约束，不改变任何用户可见的 CLI 行为、命令或接口；README 的 "AI-friendly by construction" 概览已指向 AGENTS.md，细节由 AGENTS 层承载（目录职责见 collaboration.md §5）。

---

# 批次二（AI 复合需求分解首批样例）

## 变更摘要

维护者下达复合需求（死逻辑重构 + WebUI agent 模式改版 + 工具扩展 + 准则更新），按当日新公示的「复合需求自动分解」准则（本批次第一单元即写入该准则）拆分为 4 个独立单元：准则 docs（#35）、工具死逻辑 refactor（#36）、WebUI agent 模式重构（#37）、只读工具面扩充 feat（#38），一单元一 Issue 一 PR。

## 变更类型：docs（协作准则）

| 文件                      | 变更                                                                                  |
| ------------------------- | ------------------------------------------------------------------------------------- |
| `AGENTS/collaboration.md` | §3 新增「复合需求自动分解」小节；英文 TL;DR 第 3 条扩展；自检清单新增分解公示勾选项   |
| `AGENTS.md`               | 新增黄金规则 9（复合需求先分解、一单元一 Issue 一 PR）；Change checklist 新增对应条目 |
| `changelog/2026-09-01.md` | 本批次记录 + 补记批次间落地的构建修复（见下）                                         |
| `CHANGELOG.md`            | Unreleased → Added 新增准则条目                                                       |

## 相关 Issue/PR

- Issue: #35（批次单元：#35 准则 / #36 死逻辑 / #37 WebUI / #38 工具扩展）
- PR: Closes #35（本文件所属 PR）
- 补记（fix·构建，落地于本日批次之间）：#33 / PR #34 —— `pnpm build` 现已同时构建 `@tau/tui`、`@tau/webui`（vite），CI Smoke 红灯（继承自 main 的预存缺陷）消除。

## 影响范围简述

- 仅影响 AI 协作流程文档，不触及任何运行时代码；安全模型无变化。
- 新准则自本日起对 AI 参与的所有复合任务生效；#36/#37/#38 即按此准则拆分的后续单元。

## 变更类型：refactor（工具死逻辑清理 · 分解单元 2/4）

| 文件                                      | 变更                                                                                                                              |
| ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| `packages/tools/src/file.ts`              | 修复 `file.find` type 过滤：删除不可达分支（isFile 路径内判 `type=dir`），目录/文件分别按 type 判定（`--type file` 不再列出目录） |
| `packages/tools/src/text.ts`              | 删除与 `PRUNE_DIRS` 重复的 `SKIP_DIRS`，改为导入 file 族共享集合（行为不变）                                                      |
| `packages/tools/tests/tools-file.test.ts` | 补 `type=file` / `type=dir` 回归测试（原对该参数零覆盖）                                                                          |
| `CHANGELOG.md`                            | Unreleased → Fixed 片段（#36）                                                                                                    |

## 相关 Issue/PR

- Issue: #36；PR: `Closes #36`（分解单元 2/4，批次总览见单元 1 的每日段落）

## 影响范围简述

- 行为变化仅限死逻辑本身：`tau file find --type file` 的目录误列（bug 修复）；其余工具行为不变；安全模型无变化。
- 审计确认 registry 全部导出均有真实调用方（非死代码）；`net.fetch` 的 `method` 参数风险语义问题已记录于 #36，留待维护者裁定。

## 变更类型：feat（WebUI agent 模式 · 分解单元 3/4）

| 文件                                                          | 变更                                                                                                                                  |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| `app/webui/client/lib/markdown.ts` + `tests/markdown.test.ts` | 新增零依赖、转义优先的 markdown 渲染器（DOM-free）+ 13 个测试                                                                         |
| `app/webui/client/composables/plan-flow.ts`                   | 重构为线程化 agent 状态（本地会话，localStorage 持久化）                                                                              |
| `app/webui/client/components/`                                | 新增 SessionSidebar / ShortcutsModal；Composer 升级 textarea；PlanCard 接入 markdown；ResultCard 增加 rendered/raw 切换 + 复制 + 展开 |
| `app/webui/client/App.vue` + `theme.css`                      | 三栏 agent 布局（线程 / 会话流 / 参考栏可折叠）+ 全局快捷键 + 用户气泡 + `.md-body` / `.tau-kbd` 全局样式                             |
| `app/webui/src/server.ts`                                     | `GET /api/history?limit=`（默认 20，上限 500）                                                                                        |
| `app/webui/README.md` + `app/webui/SKILL.md`                  | 客户端栈 / 布局契约 / 键盘契约 / 组件清单同步                                                                                         |
| `CHANGELOG.md`                                                | Unreleased → Added 片段（#37）                                                                                                        |

## 相关 Issue/PR

- Issue: #37；PR: `Closes #37`（分解单元 3/4，批次总览见单元 1 的每日段落）

## 影响范围简述

- 仅前端 + 一处只读 API 参数；不触及 `runPlan` 通道 / `safety.ts` / 风险语义；零新运行时依赖。
- 跨轮 AI 上下文注入为架构级变更，明确非目标（Issue #37 已声明，需另开 Issue）。

## 变更类型：feat（只读工具面扩充 · 分解单元 4/4）

| 文件                                        | 变更                                                                                                                                          |
| ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/tools/src/file.ts`                | 新增 `file.read`（行号 + offset/limit + 二进制守卫 + 2MB 上限）、`file.list`（单目录非递归清单）；`isProbablyBinary` 上移至此并与 text 族共享 |
| `packages/tools/src/sys.ts`                 | 新增 `sys.datetime` / `sys.which`（PATH 解析，不执行）/ `sys.env`（单变量读取，medium——env 可能含敏感值）                                     |
| `packages/tools/src/text.ts`                | 新增 `text.hash`（sha256/sha1，path 与 text 二选一）                                                                                          |
| `app/cli/src/file.ts` `sys.ts` `text.ts`    | CLI 子命令接线（`tau file read/list`、`tau sys datetime/which/env`、`tau text hash`）                                                         |
| `packages/tools/tests/*.test.ts`            | 每工具成功/参数校验/守卫测试（+11 个用例）                                                                                                    |
| `packages/tools/README.md` + `CHANGELOG.md` | 工具族清单与 Unreleased → Added 片段（#38）                                                                                                   |

## 相关 Issue/PR

- Issue: #38；PR: `Closes #38`（分解单元 4/4，批次总览见单元 1 的每日段落）

## 影响范围简述

- 核心工具 14 → 20 个（新增 6 个，全部只读语义，无任何写/删原语）；medium 由 `file.rename`、`text.replace`（变更类固有）与新增的 `sys.env`（信息暴露面）共同承担，其余 low。（勘误 2026-09-01 单元 A 审计：原文误记「13 → 19」与「`sys.env` 为唯一 medium」，工具计数为 14→20，medium 共三个。）
- 安全模型无变化：风险分级、dry-run 语义、无删除原语全部保持；catalog 与文档同步。

---

# 统一根级 SKILL.md（维护者指令：dev-tool 指定指导统一抽离）

## 变更摘要

维护者要求「在根文件统一抽离一个 SKILL.md 文件，对不同开发工具的使用做指定的指导」：新增仓库根级 `SKILL.md`（`tau-dev-router`）作为 SKILL.md 阅读链顶端的路由文件——每类开发工具（构建 / 测试 / 发布 / 技能编写 / WebUI 设计 / CLI 工具模块 / AI provider / 插件 / TUI）在路由表中恰有一条指定技能文件 + 规范文档 + 已验证快捷命令，并沉淀 15 秒要点、放置模型（根路由位于三层知识层之上）与「新增/移除技能同 PR 维护路由行」规则。

## 变更类型：docs（技能路由）

| 文件                      | 变更                                                                                          |
| ------------------------- | --------------------------------------------------------------------------------------------- |
| `SKILL.md`（新建，根级）  | 阅读链图 + 九类开发工具路由表（指定技能 + 规范 + 快捷命令）+ 每工具要点 + 放置模型 + 维护规则 |
| `AGENTS.md`               | 顶部读链指引新增根级 SKILL.md 路由指针                                                        |
| `AGENTS/skills.md`        | 层级模型顶部固化 root router 位置（索引，非知识层）；inventory 与放置规则同步                 |
| `CHANGELOG.md`            | Unreleased → Added 条目（#43）                                                                |
| `changelog/2026-09-01.md` | 本节                                                                                          |

## 相关 Issue/PR

- Issue: #43；PR: `Closes #43`
- 背景批次：本变更独立于批次二（#35-#38/#39-#42），为维护者当日追加指令。

## 影响范围简述

- 纯文档变更：零运行时代码改动，安全模型无变化；既有技能文件一律不移动、不改名（L3 产品内容不动）。
- 路由表中的每条链接与快捷命令均已对照实际实现核验（`app/cli/src/*.ts` 命令注册、根 `package.json` scripts、`.claude/skills/` 与 L2 技能文件位置）。

---

# 单元 A：结构审计与清单清理（维护者指令：审查删除冗余文件、理清项目结构）

## 变更摘要

维护者复合需求第一单元（分解公示：Issue #45 审计 / #46 工具开发技能 / #47 阶段规范，合并顺序 A→B→C）。对全部 186 个受控文件做引用审计：**结论为零可删文件**——12 个 WebUI 组件、全部包源码、配置点、技能资产均有真实引用（含易误判的 SidePanel 与 SessionSidebar，二者皆为活组件）。真实冗余在清单与文本：删除 4 个未使用依赖声明；结构图补全（根 SKILL.md、changelog/、app 层技能）；修复失效路径引用；README 计数与功能表过时修正；engine README 判定词表纠偏；勘误本日 changelog 两处事实错误。

## 变更类型：chore + docs（审计落地）

| 文件                            | 变更                                                                                                                                                                                                               |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `app/tui/package.json`          | 移除未使用的 `@tau/ai`、`@tau/skills` 依赖（`@tau/agent` 自行声明，黄金规则 4 归位）                                                                                                                               |
| `app/webui/package.json`        | 同上（server.ts 仅 import core/engine/agent）                                                                                                                                                                      |
| `AGENTS.md`                     | 仓库地图补 `SKILL.md`、`changelog/`；`.claude/skills` 行补 app 层技能位置                                                                                                                                          |
| `AGENTS/architecture.md`        | 治理表：根行加 `SKILL.md` 路由；删除「there is NO root SKILL.md」；L2 行补 `app/webui/SKILL.md`；新增 changelog 行                                                                                                 |
| `docs/architecture.md`          | 同步「no root SKILL.md」过时表述与 L2 清单                                                                                                                                                                         |
| `AGENTS/conventions.md`         | 修复「public API from app/cli/src/index.ts」错误路径 → 各包自身 src/index.ts                                                                                                                                       |
| `AGENTS/testing.md`             | 修复 `tests/integration/cli.test.ts` 路径；`tests/fixtures/` 不存在改为运行时生成描述                                                                                                                              |
| `AGENTS/plugins.md`             | 修复 `tests/integration/plugins-cli.test.ts` 路径                                                                                                                                                                  |
| `packages/engine/README.md`     | 判定词表 `allow/confirm/blocked` → 代码真实的 `allow/review/deny`；Workspace 补 `@tau/ui`                                                                                                                          |
| `README.md` + `README.zh-CN.md` | 测试徽章/计数 233 → 274；功能表补 #42 六工具（file read/list、sys datetime/which/env、text hash）；安装命令修正（`pnpm --filter link` 实测报错 → `cd app/cli && pnpm link --global`）；布局图补 SKILL.md/changelog |
| `changelog/2026-09-01.md`       | 勘误批次二单元 4 段两处事实错误（14→20；medium 共三个）+ 本节                                                                                                                                                      |
| `CHANGELOG.md`                  | Unreleased → Fixed 条目                                                                                                                                                                                            |

## 相关 Issue/PR

- Issue: #45；PR: `Closes #45`（分解单元 1/3，批次三总览见本文件上方批次二说明与 Issue #45 正文）

## 影响范围简述

- 零源码改动；4 个依赖声明移除经 pnpm install 锁文件更新 + 全量构建（11/11 包 dist）+ CLI 冒烟验证。
- 安装命令修正经沙箱实测：`pnpm --filter @tau/cli link` 报 `Unknown option: 'recursive'`；`cd app/cli && pnpm link --global` 为正确流程（首次需 `pnpm setup`）。
- 安全模型无变化；测试计数 233→274 已对照 vitest 实际用例数核验。
