{
  "name": "@tau/docs",
  "version": "0.6.0",
  "private": true,
  "description": "Tau documentation site — VitePress, zh (default) + en locales, content decomposed from the project's own feature map",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "catalog:",
    "vue": "catalog:"
  }
}
